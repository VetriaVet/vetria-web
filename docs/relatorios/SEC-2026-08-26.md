# Auditoria de segurança — 26/08/2026

**Escopo:** revisão pré-aplicação da migration `0002_nucleo.sql` (T-001), contra
`docs/06-PERMISSOES.md`, `supabase/migrations/0000_baseline.sql`, DL-044 a DL-047,
`middleware.ts`, `lib/auth/painel.ts` e `app/api/admin/*`.
**Base:** commit `2846ec2` · **Estado da migration:** **não aplicada**.

---

## Resumo

A migration fecha corretamente o furo que ela se propôs a fechar: depois dela, **nenhum
`vet` ou `clinic` consegue escrever o próprio `status`**. Mas ela deixa aberto o **efeito**
que o `status` existia para impedir: qualquer conta com `status = 'active'` (ou seja,
**qualquer `tutor` recém-cadastrado**) pode inserir uma linha em `vet_profiles` e aparecer
na busca pública, com CRMV inventado, sem validação e sem pagar. E como RLS é row-level e
não column-level, **`whatsapp`, `telefone`, `email_contato` e `documento_path` de todo
profissional ativo ficam legíveis por `anon`** com uma única chamada PostgREST, o que anula
o DL-047 inteiro antes de ele nascer.

**Não pode ser aplicada hoje na forma em que está.** São dois 🔴 e quatro 🟠, todos com
correção pequena e local dentro do próprio arquivo.

---

## Respostas diretas às perguntas A–G

### A. Escalada de privilégio para `status = 'active'`

**Pela coluna `status`: NÃO existe caminho. Confirmado.** Percorri os quatro vetores:

1. **Policies de UPDATE em `profiles` após 7.1/7.2.** Sobram exatamente duas:
   `profiles_update_all_master` (`using`/`with check` = `is_master_admin()`) e
   `profiles_update_own_safe_fields`, cujo `WITH CHECK` agora inclui
   `status = (select p2.status ... where p2.id = auth.uid())`. Policies permissivas se somam
   por OU: um `vet` reprova na primeira e, ao tentar mudar `status`, reprova na segunda.
   `UPDATE profiles SET status='active'` pelo cliente devolve `42501`. ✅
2. **Funções da seção 8.** `admin_definir_status` levanta `not authorized` se `is_admin()`
   for falso, e `is_admin()` só olha `role = 'admin'`, que o usuário não controla.
   `concluir_onboarding_profissional` só transiciona `incomplete → pending_validation` e
   recusa qualquer outro `meu_status`; não recebe argumento nenhum, então não há entrada a
   envenenar. ✅
3. **`handle_new_user` (seção 9).** Lê `raw_user_meta_data`, que **é** controlado pelo
   usuário no `signUp`, mas o `status` é derivado do `role_final`, e `role_final` já filtra
   `admin` fora. Não há como pedir `status` pelo metadata. O trigger é `AFTER INSERT`, então
   um `auth.updateUser({data:...})` posterior não o refaz. ✅
4. **`/api/onboarding/set-role`, `/api/admin/set-access`, `/api/admin/profiles`.** Nenhuma
   escreve `status`. `set-access` é guardada por `admin_level === 'master'` no servidor. ✅

**Mas o objetivo que a coluna protege é derrotado por outra porta.** Ver **SEC-001**: o
usuário não precisa do `status` dele para aparecer na busca; ele só precisa de **uma conta
qualquer que já nasça `active`**, e o `tutor` nasce `active` por decisão da própria seção 9.
Como `perfil_esta_ativo()` checa **só `status`, nunca `role`**, e `vet_profiles_insert_own`
checa **só `id = auth.uid()`, nunca `role`**, a linha entra e fica pública. Isso é 🔴 e é
literalmente o cenário da §3 regra 1 da matriz, alcançado sem tocar em `status`.

### B. Recursão de RLS (DL-014/015)

**Não há recursão. Confirmado, com uma ressalva.**

- `is_admin()` (2.1), `perfil_esta_ativo()` (2.2) e `is_master_admin()` (baseline 3.1) são
  todas `security definer set search_path = public`. `SECURITY DEFINER` troca o
  `current_user` para o dono da função (`postgres`), que é o dono de `public.profiles`, e
  como `FORCE ROW LEVEL SECURITY` está **desligado** (baseline §2), a RLS não é reaplicada
  dentro delas. O laço fecha. ✅
- **O `alter policy` da 7.2 é o ponto que merecia a pergunta, e ele está correto.** A
  subquery `(select p2.status from public.profiles p2 where p2.id = auth.uid())` está dentro
  de uma policy **de `profiles`**, e ela **de fato** dispara avaliação de RLS sobre
  `profiles`. Mas o que dispara são as policies de **SELECT**, não a de UPDATE em que ela
  vive. As policies de SELECT são `profiles_select_own` (`id = auth.uid()`, expressão pura),
  `profiles_select_all_master` (`is_master_admin()`, DEFINER) e a nova
  `profiles_select_admin` (`is_admin()`, DEFINER). **Nenhuma delas consulta `profiles` em
  modo INVOKER**, então a recursão termina em um nível. Prova empírica adicional: esse mesmo
  padrão de subquery já está em produção hoje em `profiles_update_own_safe_fields`
  (baseline §5) e o banco não cai.
- **Ressalva (SEC-011, 🟡):** essa segurança é condicional, não estrutural. Se alguém um dia
  ligar `FORCE ROW LEVEL SECURITY` em `profiles`, ou adicionar uma policy de SELECT em
  `profiles` que consulte `profiles` sem `DEFINER`, o DL-014 volta na hora — e volta na forma
  de banco fora do ar, não de erro de teste.
- **Sobra viva:** a 0002 remove `current_user_role()` (seção 11), que era a arma carregada.
  Isso está certo e fecha metade do R-005. ✅

### C. Vazamento entre painéis

Célula por célula contra a §3:

| Pergunta | Linha (row-level) | Coluna (column-level) |
|---|---|---|
| Um `vet` lê `clinic_profiles` de outro? | **Só se `active`** — e a matriz manda que perfil ativo seja público. ✅ correto | ❌ **lê também `telefone`, `whatsapp`, `email_contato` e `documento_path`** |
| Um responsável lê `vet_profiles` de quem não está `active`? | **Não.** `perfil_esta_ativo(id)` é falso, a linha some. ✅ | — |
| Um profissional em `pending_validation` aparece em leitura pública? | **Não.** ✅ | — |
| Um `vet` lê `animais` de um tutor? | **Não.** `animais_select_own` é `tutor_id = auth.uid()`, e nem admin lê. ✅ | — |
| Um profissional lê `contatos` de outro profissional? | **Não.** `profissional_id = auth.uid()`. ✅ | — |
| Um `vet` lê `audit_logs`? | **Não.** Só `is_master_admin()`. ✅ | — |

**O isolamento por linha está certo. O isolamento por coluna não existe** — e é aí que mora
o vazamento real. RLS não tem granularidade de coluna; a migration não emite nenhum
`GRANT SELECT (col, col, ...)`, e no Supabase o `anon` recebe `SELECT` na tabela inteira por
default privilege. Ver **SEC-002**.

Um detalhe que **não** é vazamento e vale registrar: a §2 da matriz e o DL-044 dizem que a
busca e o perfil público são abertos a qualquer role, logado ou não. As policies
`*_select_publico` estão `to anon, authenticated`, o que codifica isso corretamente. O
isolamento de painel continua sendo problema de `middleware.ts` (R-001), fora do escopo
desta migration.

### D. A remoção da `profiles_update_own_safe` (7.1)

**É segura, e é mais que segura: é obrigatória. A justificativa escrita está correta, mas
incompleta em um ponto que importa.**

Raciocínio próprio, em duas partes.

**Parte 1 — antes da 7.2, a policy removida era mesmo estritamente menos permissiva.**
Compare os dois `WITH CHECK`. O da `_safe` exige `admin_level = (subselect)`. O da
`_safe_fields` exige `coalesce(admin_level,'none') = coalesce((subselect),'none')`. Dois
casos:

- ambos os lados não-nulos → `x = x` é `true`, e `coalesce(x)=coalesce(x)` também. As duas
  passam. Empate.
- qualquer lado nulo → `NULL = NULL` resolve para `NULL`; um `WITH CHECK` de RLS exige
  `true`, e `NULL` **não é** `true`, então a `_safe` reprova. A `_safe_fields` passa, porque
  o `coalesce` converteu o `NULL` em `'none'`.

Logo o conjunto de linhas aceitas pela `_safe` é subconjunto próprio do aceito pela
`_safe_fields`. E o `USING` das duas é idêntico (`id = auth.uid()`). Como policies
permissivas se somam por OU, remover um subconjunto de um OU não altera o resultado.
**Confirmado: a remoção, isolada, não muda nada.** E o caso do lado nulo não é teórico — a
baseline §7 registra uma linha real em produção com `admin_level` NULL, vinda de
`/api/admin/set-access:63` (`admin_level: null`).

**Parte 2 — depois da 7.2, ela deixa de ser subconjunto, e mantê-la seria o furo.** A 7.2
acrescenta `and status = (select p2.status ...)` **só** à `_safe_fields`. A `_safe` continua
sem mencionar `status`. Um `UPDATE profiles SET status='active' WHERE id = auth.uid()` de um
vet com `admin_level`/`admin_team` não-nulos (que é o estado de todos eles depois da seção
10.3) reprovaria na `_safe_fields` e **passaria na `_safe`** — e o OU aceitaria. A 7.2
viraria decoração. **Confirmado: a remoção não é faxina, é pré-condição do 7.1 para o 7.2
valer.**

**O ponto que falta na justificativa do arquivo:** ela argumenta a partir do `WITH CHECK` e
não menciona o `USING`. Vale registrar explicitamente que os dois `USING` são idênticos,
porque se o da `_safe` fosse mais amplo a conclusão de "subconjunto" cairia. São idênticos,
então a conclusão se sustenta. E o bloco de reversão (seção 13) recria a `_safe` **junto
com** a restauração do `WITH CHECK` da `_safe_fields` sem `status`, o que mantém o par
consistente. Reversão correta. ✅

### E. `contatos` sem INSERT e a ausência geral de DELETE

**`contatos` sem policy de INSERT: intencional e correto.** DL-047 manda o registro ser
evento de servidor. Sem policy, `anon` e `authenticated` recebem `42501` em qualquer
`INSERT` via PostgREST; o `service_role` ignora RLS e grava normalmente. **Fail-closed, que
é a direção certa.**

**O que a F4/S8 vai precisar e não vai conseguir fazer pelo cliente (por design, mas precisa
estar escrito no card, senão o dev descobre na hora errada):**

- o `POST` que grava o contato **tem** de usar `SUPABASE_SERVICE_ROLE_KEY`, nunca o cliente
  de sessão;
- a vinculação do passo 4 do DL-047 (`UPDATE contatos SET user_id = <novo> WHERE anon_id =
  <cookie>`) **também** só roda com service role, porque **não existe policy de UPDATE em
  `contatos`**. Isso é pior que um `INSERT` negado: um `UPDATE` bloqueado por RLS não levanta
  erro, **devolve zero linhas afetadas em silêncio**. Se alguém implementar isso com o
  cliente de sessão, o histórico anônimo simplesmente nunca aparece, e nada no log acusa;
- a revelação do número precisa ler `vet_profiles.whatsapp` no servidor — o que hoje é
  inútil, porque o `anon` já lê essa coluna direto (SEC-002).

**Ausência de DELETE: correta em `contatos` e `audit_logs`** (integridade de trilha; um admin
que apaga o próprio rastro é pior que não ter trilha). **Aceitável, mas com dívida marcada,
em `vet_profiles`/`clinic_profiles`/`profiles`**: a exclusão de conta da LGPD (F6) vai
precisar disso, e hoje o `on delete cascade` de `profiles` resolve o caso desde que a
exclusão passe por service role.

**O que a F3/S4 vai precisar e NÃO vai conseguir fazer — e isso é achado, não nota:**

1. **Moderar conteúdo.** DL-045 e a §5 dão "moderar conteúdo ✅" ao admin comum, e a §3 diz
   que `vet_profiles`/`clinic_profiles` são escritos por "o dono, **admin**, master". A
   migration dá ao admin **apenas `SELECT`**. Não há `UPDATE` nem `DELETE` de admin. Admin
   não derruba bio ofensiva, não corrige CRMV, não remove perfil fraudulento. Ver
   **SEC-006**.
2. **Reprovar com motivo visível.** A §4 desenha `admin reprova (com motivo)` e a tela
   `/aguardando` do DL-046 precisa exibi-lo. `admin_definir_status` grava o `motivo` **só em
   `audit_logs`**, e `audit_logs` é legível **só pelo master**. Não existe coluna nenhuma,
   em lugar nenhum, onde o profissional possa ler por que foi reprovado. Ver **SEC-007**.
3. **Ver o documento.** Depende do bucket da T-002, que não está nesta migration. Esperado,
   mas é dependência dura da S4.

### F. `documento_path`

**Leitura: sim, vaza — para qualquer um, inclusive `anon`.** `vet_profiles_select_publico`
libera a **linha inteira** de todo perfil `active`. `documento_path` é uma coluna dessa
linha. `GET /rest/v1/vet_profiles?select=id,documento_path` com a chave anônima devolve o
caminho do documento de identidade de todos os profissionais ativos. O arquivo em si segue
protegido pelo bucket privado (T-002), então isto sozinho é 🟠 e não 🔴 — mas é a metade de
cima de uma cadeia que fecha em 🔴 junto com o item seguinte.

**Escrita: sim, o dono grava caminho arbitrário.** `vet_profiles_update_own` tem
`WITH CHECK (id = auth.uid())` e nada mais. RLS não compara valor novo com valor antigo de
coluna, e não tem granularidade de coluna. Então o dono da linha escreve **qualquer string**
em `documento_path`, inclusive o caminho do arquivo de outra pessoa — caminho que ele acabou
de ler pelo item anterior. Cadeia completa em **SEC-003**. É exatamente a regra "nome de
arquivo do usuário nunca vira caminho no servidor" quebrada na direção inversa: aqui o
caminho é 100% do usuário e o servidor vai assiná-lo.

### G. Outros achados, erros de SQL e ordem de execução

**Ordem e sintaxe: revisei linha a linha e não achei nada que impeça a transação de rodar.**
Em particular, confirmei que:

- a coluna `status` (seção 3) nasce **antes** do `alter policy` que a referencia (7.2). ✅
- `ALTER POLICY ... WITH CHECK (...)` sem `USING` é sintaxe válida e **preserva o `USING`
  existente**, que era o ponto de falha mais provável do arquivo. ✅
- `bigint generated always as identity`, `gen_random_uuid()`, `gin` sobre `text[]` com o
  `array_ops` embutido e `create type` dentro de transação: todos válidos no Postgres do
  Supabase. ✅
- `REVOKE ... FROM anon, public` interpreta `public` como o pseudo-papel PUBLIC, que é o
  pretendido. ✅
- a migration inteira é uma transação; falha em qualquer ponto reverte tudo. Não há estado
  intermediário possível. ✅ Consequência boa: como nada tem `if not exists`, uma segunda
  execução acidental **falha inteira** em vez de corromper. Fail-closed.

**O que encontrei além disso** está em SEC-004, SEC-005 e SEC-008 a SEC-013. Dois merecem
destaque aqui porque não são de segurança e sim de "não vai rodar / vai rodar diferente do
esperado":

- **A única linha que pode derrubar a transação inteira** é a 508:
  `revoke execute on function public.admin_set_user_access(uuid, public.user_role, public.admin_level, public.admin_team) from anon;`.
  `REVOKE` numa função exige assinatura **exata**; assinatura que não casa dá
  `function does not exist` e **a 0002 inteira faz rollback**, no fim de tudo. A baseline
  §3.7 documenta os **nomes** dos parâmetros, não os tipos. Ver SEC-010.
- **A seção 10.2 é incondicional** (`where role in ('vet','clinic')`, sem filtro de
  `onboarding_completed`). Hoje acerta exatamente os 6 profissionais de teste e o resultado é
  o pretendido. Mas o arquivo fica no repo, e um dia alguém o roda de novo em um banco com
  profissionais reais e **derruba todos eles para `incomplete` de uma vez**. O cabeçalho da
  0000 tem o aviso "NÃO RODAR EM PRODUÇÃO"; a 0002 não tem o equivalente. Ver SEC-012.

---

## Achados

### SEC-001 — Qualquer conta `active` publica um perfil profissional na busca, sem validação e sem pagar · 🔴

- **Onde:** `supabase/migrations/0002_nucleo.sql:79-87` (`perfil_esta_ativo`), `:243-245` e
  `:253-254` (`vet_profiles`), `:261-263` e `:271-272` (`clinic_profiles`), `:465-468`
  (`handle_new_user` dá `active` ao tutor), `:483-484` (seção 10.1).
- **O quê:** a migration codifica a visibilidade da busca como **`status = 'active'`**. A
  matriz, §3 regra 2, define a regra como **`role IN ('vet','clinic') AND status =
  'active'`**. A metade `role` desapareceu, em dois lugares que se reforçam:
  1. `perfil_esta_ativo(p_id)` consulta `profiles` e filtra **só por `status`**. Nunca olha
     `role`.
  2. `vet_profiles_insert_own` / `clinic_profiles_insert_own` têm
     `WITH CHECK (id = auth.uid())` e **nenhuma checagem de `role`**.

  E, por decisão da própria migration, **o `tutor` nasce `active`** (seção 9, ramo `else`) e
  todos os tutores existentes viram `active` (10.1). Ou seja, o predicado que deveria ser um
  portão de validação está sempre aberto para a única classe de conta que qualquer pessoa
  cria em 30 segundos, sem nenhuma revisão humana.
- **Como explorar:**
  1. `supabase.auth.signUp({ email, password })` sem `role` no metadata, ou pelo funil
     `/cadastro/responsavel`. O trigger cria `profiles` com `role='tutor'` e
     **`status='active'`**.
  2. Confirmar o email. Nada mais é exigido: nenhum documento, nenhum CRMV, nenhum admin.
  3. Com o JWT dessa sessão e a chave anônima pública:
     `POST /rest/v1/vet_profiles` com
     `{"id":"<meu uid>","nome_exibicao":"Dr. Fulano","crmv":"99999","crmv_uf":"SP","cidade":"São Paulo","estado":"SP","especialidades":["cardiologia"],"slug":"dr-fulano","whatsapp":"+5511..."}`.
     `vet_profiles_insert_own` aprova, porque `id = auth.uid()`.
  4. Deslogar. Como `anon`: `GET /rest/v1/vet_profiles?select=*` → a linha aparece, porque
     `perfil_esta_ativo('<meu uid>')` é **verdadeiro** (o tutor é `active`).
  5. Repetir o passo 3 contra `clinic_profiles` com o **mesmo uid**: nada impede a mesma
     conta de ter perfil de veterinário **e** de estabelecimento simultaneamente. Não há
     unicidade cruzada nem checagem de role.
- **Impacto:** é o furo mais caro da lista, e é pior que o originalmente temido, porque o
  atacante nem precisa de uma conta profissional. (a) **Modelo de negócio contornado por um
  INSERT**: exposição na busca é literalmente o único produto que o `vet` compra (matriz §1).
  (b) **Confiança e responsabilidade civil**: a plataforma passa a listar "veterinários" com
  CRMV inventado, para tutores que vão levar animal doente. Validação de CRMV é a promessa
  central da V1. (c) **Perversão do incentivo**: o vet honesto, que sobe documento e espera o
  admin, fica `pending_validation` e **não aparece**; o impostor aparece na hora. (d) **Sem
  remédio operacional**: `admin_definir_status` recusa alvo cujo `role` não seja
  `vet`/`clinic` (`:378-380`), então **o admin não consegue nem suspender essa conta**, e não
  tem `UPDATE`/`DELETE` em `vet_profiles` (SEC-006). A única saída é `service_role` na mão.
- **Como corrigir:** fechar nos dois lados, porque cada um sozinho é contornável.
  (1) `perfil_esta_ativo` passa a exigir também o `role` correto — o mais limpo é trocá-la
  por duas funções (`vet_esta_ativo` / `clinic_esta_ativo`) ou receber o role esperado como
  parâmetro, para que a policy de `vet_profiles` nunca aceite um `clinic` e vice-versa.
  (2) o `WITH CHECK` dos dois `*_insert_own` passa a exigir que o dono tenha o role da tabela,
  via função `DEFINER` (não dá para consultar `profiles` em modo INVOKER ali).
  (3) considerar uma constraint que impeça o mesmo `id` de existir nas duas tabelas.
- **Vira task:** T-005

### SEC-002 — Telefone, WhatsApp, email e caminho do documento de todo profissional ativo são legíveis por `anon` em uma chamada · 🔴

- **Onde:** `0002_nucleo.sql:243-245` e `:261-263` (as duas policies `*_select_publico`),
  sobre as colunas `:135-138` e `:157-161`.
- **O quê:** RLS no Postgres é **row-level, não column-level**. `USING (perfil_esta_ativo(id))`
  decide *se a linha aparece*, nunca *quais colunas*. A migration não emite nenhum
  `REVOKE SELECT` seguido de `GRANT SELECT (colunas públicas)`, e no Supabase o papel `anon`
  recebe `SELECT` na tabela inteira pelo default privilege do schema `public`. Resultado:
  `whatsapp`, `telefone`, `email_contato` e `documento_path` são colunas públicas.
- **Como explorar:** com a `NEXT_PUBLIC_SUPABASE_ANON_KEY`, que está no bundle de qualquer
  página do site, sem sessão nenhuma:
  `GET /rest/v1/vet_profiles?select=nome_exibicao,crmv,whatsapp,telefone,email_contato,documento_path&limit=10000`
  e o mesmo em `clinic_profiles`. Uma requisição, sem paginar perfil por perfil, sem
  navegador, sem parser de HTML. A base inteira de telefones sai em JSON.
- **Impacto:** **o DL-047 é anulado antes de ser implementado.** A decisão registra
  explicitamente que o link `wa.me` foi rejeitado porque "expõe a base de telefones e destrói
  a métrica que sustenta o preço do plano". A alternativa escolhida (evento de servidor +
  `contatos`) só funciona se o número **não** estiver alcançável por outro caminho. Aqui
  está, e por um caminho mais confortável que raspar HTML. Consequências em cadeia: (a) o
  concorrente leva a base de contatos comercial inteira; (b) a métrica "você recebeu 34
  contatos este mês", que é o que se vende ao profissional, passa a subcontar, porque o
  contato real acontece fora da plataforma; (c) LGPD — telefone e email de pessoa
  identificada expostos a qualquer um, sem base legal e sem registro de acesso; (d)
  `documento_path` vazado alimenta a SEC-003.
- **Como corrigir:** RLS não resolve isto; **privilégio de coluna** resolve. Para cada uma
  das duas tabelas: `revoke select on <tabela> from anon, authenticated;` seguido de
  `grant select (<só as colunas públicas>) on <tabela> to anon, authenticated;`, mantendo as
  policies como estão (privilégio de coluna e RLS se compõem por E). A alternativa é uma
  **view** com as colunas públicas, `security_invoker = off`, exposta ao `anon`, com a
  tabela-base fechada. Em qualquer das duas, `whatsapp`, `telefone` e `documento_path` ficam
  de fora, e o número passa a existir só pelo endpoint de servidor do DL-047.
- **Vira task:** T-006

### SEC-003 — O dono grava `documento_path` arbitrário e faz o admin abrir o documento de outra pessoa · 🟠

- **Onde:** `0002_nucleo.sql:256-258` e `:274-276` (`*_update_own`), colunas `:138` e `:161`.
- **O quê:** o `WITH CHECK` é só `id = auth.uid()`. Um `WITH CHECK` de RLS enxerga apenas a
  **linha nova**; não tem acesso ao valor antigo da coluna nem granularidade por coluna. Logo
  não existe nada, em lugar nenhum do schema, restringindo o conteúdo de `documento_path` ao
  prefixo do próprio usuário. É texto livre escrito pelo cliente.
- **Como explorar** (encadeia com SEC-002):
  1. Como `anon`, `GET /rest/v1/vet_profiles?select=id,documento_path` e escolher a vítima A.
     (Sem a SEC-002 o passo ainda funciona por adivinhação, porque `vet_profiles.id` **é** o
     uid e o caminho previsível é `<uid>/documento.pdf`.)
  2. Com a própria conta B, já em `pending_validation`:
     `PATCH /rest/v1/vet_profiles?id=eq.<uid de B>` com `{"documento_path":"<caminho de A>"}`.
     A policy aprova: `id = auth.uid()`.
  3. B entra na fila de validação. O admin abre a tela da S4, que vai ler `documento_path` e
     chamar `createSignedUrl()` com `service_role` (o bucket é privado, é o único jeito). A
     URL assinada aponta para o **documento de identidade de A**.
- **Impacto:** deputado confuso clássico — o admin, que tem autorização legítima, é usado
  como oráculo para abrir arquivo de terceiro. O que vaza é a categoria mais sensível do
  projeto: RG/CNH e comprovante de CRMV, dado pessoal sensível sob LGPD, e o vazamento fica
  **registrado em `audit_logs` como acesso legítimo do admin**, o que envenena a própria
  trilha. Também é vetor de negação: B aponta para um caminho inexistente e a fila trava.
- **Como corrigir:** tirar a coluna da mão do cliente. Ou (a) trigger `BEFORE UPDATE` em cada
  tabela que rejeite `new.documento_path` que não comece com `auth.uid()::text || '/'` (e que
  rejeite alteração quando o status já for `pending_validation`), ou (b) revogar
  `UPDATE(documento_path)` do `authenticated` por privilégio de coluna e deixar o caminho ser
  escrito só pela rota de upload no servidor, que já conhece o uid da sessão. A (b) é a que
  cabe junto com a T-002/T-004 sem código novo de trigger. Vale aplicar o mesmo tratamento a
  `slug` (SEC-008).
- **Vira task:** T-007

### SEC-004 — `profiles_select_admin` entrega a base inteira ao admin comum, contra a §5 da matriz · 🟠

- **Onde:** `0002_nucleo.sql:312-313`, sobre `is_admin()` em `:66-74`.
- **O quê:** a policy é `using (public.is_admin())`, sem qualquer recorte, e `is_admin()` é
  verdadeira para **qualquer** `role='admin'`, inclusive `admin_level='none'`. A §5 da matriz
  é explícita: **"Ver a base inteira de usuários — Admin ❌ · Master ✅"**, e o DL-045
  justifica ("suspender conta paga é poder demais pra nível operacional" — o mesmo raciocínio
  vale para ler a base). A necessidade real declarada é estreita: o admin comum precisa da
  **fila de validação**, que é `role IN ('vet','clinic') AND status = 'pending_validation'`.
  A policy entregue é ordens de magnitude mais larga que a necessidade.
- **Como explorar:** um admin comum recém-promovido, com a chave anônima e a própria sessão:
  `GET /rest/v1/profiles?select=id,full_name,phone,role,status,admin_level` devolve **toda**
  a base, incluindo `full_name` e `phone` de todos os tutores — pessoas que não têm relação
  nenhuma com fila de validação. Não precisa do painel: PostgREST direto.
- **Impacto:** o corte admin/master do DL-045 deixa de existir na prática para leitura, que é
  metade do que ele separava. Um operador contratado leva nome e telefone da base de
  consumidores inteira. Sob LGPD é acesso sem finalidade, e sem rastro: `SELECT` via
  PostgREST não passa por `audit_logs`.
- **Como corrigir:** estreitar a policy ao que o DL-045 concede, deixando a base inteira para
  a policy de master que já existe: `using (public.is_admin() and role in ('vet','clinic'))`,
  ou, mais fiel ainda, acrescentar `and status in ('pending_validation','active','suspended')`.
  A `profiles_select_all_master` continua cobrindo o master.
- **Vira task:** T-008

### SEC-005 — Admin comum reativa conta que o master suspendeu · 🟠

- **Onde:** `0002_nucleo.sql:362-369` (`admin_definir_status`).
- **O quê:** o guard de master é one-way. Ele testa apenas o **destino**:
  `if novo_status = 'suspended' and not is_master_admin() then raise`. Não testa a **origem**.
  Sair de `suspended` é operação de admin comum.
- **Como explorar:** o master suspende a conta X (fraude, inadimplência, ordem judicial). Um
  admin comum chama `select public.admin_definir_status('<uid de X>', 'active');` —
  `is_admin()` é verdadeira, `novo_status <> 'suspended'`, o guard não dispara, e X volta para
  a busca. O `audit_logs` registra, mas registra **depois**, e quem lê `audit_logs` é só o
  master, que não tem alerta nenhum.
- **Impacto:** a suspensão, que o DL-045 reservou ao master exatamente porque "suspender
  conta paga é poder demais pra nível operacional", vira reversível por qualquer operador.
  Desfazer uma decisão de governo é o mesmo poder que tomá-la, com o agravante de que quem a
  tomou não é notificado.
- **Como corrigir:** o guard passa a olhar os dois lados:
  `if (novo_status = 'suspended' or status_antigo = 'suspended') and not is_master_admin()`.
  Isso exige mover a leitura de `status_antigo` para **antes** do guard, o que é uma troca de
  ordem de duas instruções dentro da mesma função.
- **Vira task:** T-009

### SEC-006 — Admin não consegue moderar nem remover perfil profissional · 🟠

- **Onde:** `0002_nucleo.sql:240-276`. Existem `vet_profiles_select_admin` e
  `clinic_profiles_select_admin`, e **nenhuma** policy de `UPDATE` ou `DELETE` para admin.
- **O quê:** a §3 da matriz diz que `vet_profiles`/`clinic_profiles` são escritos por "o dono,
  **admin**, master". A §5 e o DL-045 dão "moderar conteúdo ✅" ao admin comum. A migration
  concede **só leitura**. Não é ausência por descuido de escopo: as policies de admin foram
  escritas, e só a metade de leitura entrou.
- **Como explorar:** não é exploração de atacante, é ausência de capacidade defensiva, com
  passos igualmente concretos. Um perfil ativo publica bio ofensiva, CRMV de terceiro ou
  telefone de golpe. O admin abre `/admin/moderacao`, e qualquer
  `PATCH /rest/v1/vet_profiles?id=eq.<x>` ou `DELETE` com a sessão dele devolve zero linhas
  afetadas — silenciosamente, porque `UPDATE`/`DELETE` barrados por RLS não levantam erro. O
  único caminho que resta é `admin_definir_status(x,'suspended')`, que (a) exige **master**,
  (b) não apaga o conteúdo, só o esconde, e (c) **não funciona** contra o perfil criado por um
  `tutor` da SEC-001, porque a função recusa alvo que não seja `vet`/`clinic`.
- **Impacto:** a plataforma não tem botão de desligar conteúdo abusivo dentro do próprio
  produto. Toda moderação vira intervenção manual com `service_role` no dashboard, fora de
  qualquer trilha de auditoria — o oposto do que o DL-045 desenhou. Bloqueia o DoD da F3/S4.
- **Como corrigir:** acrescentar `*_update_admin` (`using`/`with check` = `is_admin()`) e
  decidir conscientemente sobre remoção: ou uma policy de `DELETE` para master, ou uma coluna
  de ocultação (`oculto boolean`) que o admin marca e que as policies `*_select_publico`
  passam a respeitar. A segunda preserva a evidência, que é o que se quer em moderação.
- **Vira task:** T-010

### SEC-007 — O motivo da reprova só existe em `audit_logs`, que o reprovado nunca lê · 🟡

- **Onde:** `0002_nucleo.sql:386-393` (o `motivo` vai para `audit_logs.detalhe`) e `:308-309`
  (`audit_logs` só é legível por `is_master_admin()`).
- **O quê:** a §4 da matriz desenha "admin reprova (com motivo)" e a tabela de status manda o
  suspenso ver "tela de bloqueio **com motivo**". O DL-046 dá ao `pending_validation` a rota
  `/aguardando`. Não existe nenhuma coluna, em `profiles` nem em `vet_profiles`, que guarde o
  motivo corrente de reprova ou suspensão de forma legível pelo próprio titular.
- **Como explorar:** não há exploração — é por isso que é 🟡 e não 🟠. É bloqueio funcional da
  S4/S5, verificável em um passo: `admin_definir_status(x,'incomplete','CRMV ilegível')` e em
  seguida, logado como x, não existe query que devolva a string "CRMV ilegível". Nem
  `audit_logs` (master-only), nem `profiles`.
- **Impacto:** o profissional é devolvido para `incomplete` sem saber o que corrigir, refaz o
  mesmo cadastro e é reprovado de novo. Custo de suporte por profissional, no exato momento do
  funil em que ele decide se fica. Também tem leitura de LGPD: transparência sobre decisão que
  afeta o titular.
- **Como corrigir:** uma coluna `status_motivo text` em `profiles`, escrita **só** por
  `admin_definir_status`, incluída na fixação da 7.2 (o dono não pode reescrever o próprio
  motivo) e legível pelo dono via `profiles_select_own`, que já existe. Custo: uma coluna e um
  termo a mais no `WITH CHECK`.

### SEC-008 — `slug` é escrito livremente pelo dono, antes de existir regra de slug · 🟡

- **Onde:** `0002_nucleo.sql:121` e `:146` (`slug text unique`), liberados por `*_update_own`
  (`:256-258`, `:274-276`).
- **O quê:** o próprio comentário do arquivo diz "nulo até virar 'active' (F4/S5 define a
  regra)", mas nada no schema impõe isso. O dono escreve qualquer string, a qualquer momento,
  em qualquer status. `unique` é por tabela, então o mesmo slug pode existir em `vet_profiles`
  e em `clinic_profiles` ao mesmo tempo — hoje sem colisão de rota, mas sem garantia.
- **Como explorar:** combinado com a SEC-001, uma conta `tutor` de 30 segundos faz
  `PATCH /rest/v1/vet_profiles` com `{"slug":"<nome da clínica alvo>"}` e **queima o slug**
  para sempre, porque o índice é único e não há admin com `UPDATE` para liberá-lo (SEC-006).
  Squatting de identidade em massa, roteirizável, custo zero.
- **Impacto:** identidade de URL pública, que é ativo de SEO e de marca, entregue por ordem de
  chegada a contas não validadas, sem caminho de recuperação dentro do produto.
- **Como corrigir:** mesma direção da SEC-003 — tirar `slug` da escrita do cliente. Ele deve
  ser gerado no servidor no momento em que o admin aprova (que é o que a F4/S5 já pretendia),
  por privilégio de coluna ou trigger.

### SEC-009 — `perfil_esta_ativo()` é executável por `anon` e vira oráculo de enumeração · 🟡

- **Onde:** `0002_nucleo.sql:79-87`. A seção 8 revoga `EXECUTE` de `anon` nas funções novas
  (`:432-435`) e a seção 11 nas duas RPC herdadas (`:507-508`), mas `perfil_esta_ativo` e
  `is_admin` ficam com o `EXECUTE` para PUBLIC que é o default.
- **O quê:** `perfil_esta_ativo` é `SECURITY DEFINER` e responde sobre um uuid **arbitrário**,
  não sobre o chamador. Isso a torna chamável como RPC por qualquer um.
- **Como explorar:** `POST /rest/v1/rpc/perfil_esta_ativo` com `{"p_id":"<uuid>"}` e a chave
  anônima devolve `true`/`false` para qualquer uuid, revelando existência e estado de contas
  que a RLS de `profiles` esconde — incluindo tutores e admins, que nunca aparecem em
  `vet_profiles`. É oráculo, não dump: exige o uuid de antemão, e por isso é 🟡. Mas uuids
  vazam com facilidade (`vet_profiles.id` é o uid, e a SEC-002 os entrega em lote).
- **Impacto:** confirmação de existência de conta e do estado dela para quem não deveria
  conseguir nem enxergar a linha. Superfície desnecessária: a função existe para ser chamada
  **de dentro de uma policy**, nunca pela rede. A própria migration já aplica esse princípio às
  outras funções e simplesmente não o aplicou a estas duas.
- **Como corrigir:** `revoke execute on function public.perfil_esta_ativo(uuid) from anon,
  public;` e o mesmo para `public.is_admin()`. Policies continuam funcionando: a avaliação
  interna de policy não depende de `EXECUTE` do chamador.

### SEC-010 — O `revoke` da linha 508 pode derrubar a migration inteira no último passo · 🟡

- **Onde:** `0002_nucleo.sql:507-508`.
- **O quê:** `REVOKE ... ON FUNCTION` exige a assinatura exata; assinatura que não casa é
  `ERROR: function ... does not exist`. A baseline §3.7 documenta os **nomes** dos parâmetros
  de `admin_set_user_access` mas **não os tipos**, e o arquivo assume
  `(uuid, public.user_role, public.admin_level, public.admin_team)`. Se houver `default`,
  ordem diferente ou um `text` no lugar de um enum, a instrução falha.
- **Como explorar:** não é vetor de ataque, é modo de falha operacional, e é ruim porque está
  **no fim** de uma transação longa: tudo roda, o Elber vê o SQL avançar, e o rollback vem no
  fim sem que nada tenha sido aplicado. Diagnóstico confuso em janela de manutenção.
- **Impacto:** custo de sessão presencial, não de segurança. O rollback em si é o
  comportamento correto (fail-closed).
- **Como corrigir:** antes de rodar, conferir a assinatura real com
  `select p.oid::regprocedure from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname in ('admin_set_user_access','admin_list_profiles');`
  e colar o resultado literal nas duas linhas. Custa 10 segundos e elimina o único modo de
  falha total do arquivo.

### SEC-011 — A não-recursão do 7.2 é condicional, e nada no schema a protege · 🟡

- **Onde:** `0002_nucleo.sql:329-338`, `:312-313`, `:66-74`; baseline §2 e §3.1.
- **O quê:** conforme demonstrado na resposta B, hoje não há recursão. Mas isso depende de
  duas condições que não estão declaradas nem protegidas em lugar nenhum: (1) `FORCE ROW LEVEL
  SECURITY` continuar **desligado** em `profiles`, e (2) nenhuma policy de **SELECT** em
  `profiles` passar a consultar `profiles` fora de uma função `DEFINER`. A 0002 aumenta a
  exposição a (2), porque acrescenta uma terceira policy de SELECT em `profiles`
  (`profiles_select_admin`) e normaliza o padrão "policy de profiles que consulta profiles".
- **Como explorar:** não é atacável de fora. É armadilha para a próxima sessão, e o DL-014
  registra que ela já foi pisada uma vez, com o banco fora do ar como resultado.
- **Impacto:** indisponibilidade total do banco, com erro (`infinite recursion detected in
  policy for relation "profiles"`) que só aparece em runtime e não em revisão de SQL.
- **Como corrigir:** comentário explícito acima da 7.2 e da `profiles_select_admin`
  registrando as duas condições, e um item permanente no checklist do `vetria-seguranca`: toda
  policy nova em `profiles` que mencione `profiles` passa por `DEFINER` ou não entra. Barato, e
  é o único tipo de defesa que existe contra isso.

### SEC-012 — A 0002 não tem trava contra reexecução, e a seção 10.2 é destrutiva · 🟡

- **Onde:** `0002_nucleo.sql:1-15` (cabeçalho, sem aviso de "já aplicada") e `:489-491`.
- **O quê:** a 10.2 roda `update profiles set status='incomplete', onboarding_completed=false
  where role in ('vet','clinic')`, **sem nenhum filtro**. Hoje isso acerta os 6 profissionais
  de teste e o efeito é o pretendido (decisão B). O problema é o arquivo permanecer no repo sem
  o aviso que a 0000 tem em destaque ("DOCUMENTAL. JÁ APLICADO. NÃO RODAR EM PRODUÇÃO").
- **Como explorar:** cenário concreto e nada exótico: mês 5, alguém recria ambiente ou roda a
  pasta `migrations/` inteira contra produção. As instruções `create type` falham primeiro e a
  transação reverte — então **hoje** o fail-closed segura. Mas basta alguém executar a 10.2
  isoladamente, ou tornar os `create` idempotentes, para que ela devolva **todos** os
  profissionais pagantes a `incomplete` e os tire da busca de uma vez.
- **Impacto:** todos os profissionais ativos somem da busca simultaneamente, e o
  `onboarding_completed` original **não é recuperável pela reversão da seção 13** — o próprio
  arquivo admite isso e manda usar o backup.
- **Como corrigir:** cabeçalho igual ao da 0000 assim que ela for aplicada, e restringir a 10.2
  ao alvo real com `and onboarding_completed = true`, para que ela não tenha efeito em uma
  execução futura.

### SEC-013 — A matriz §1 registra um valor de `admin_level` que o enum não aceita · ⚪ (correção de documentação)

- **Onde:** `docs/06-PERMISSOES.md` §1 (linha do Admin, `admin_level` = `comum` ⚠️) contra
  `0000_baseline.sql:32` (`create type public.admin_level as enum ('none','admin','master')`).
- **O quê:** a matriz marca esse valor como "precisa ser confirmado contra o enum real" e a
  T-000b tinha como critério "R-002 item 3 resolvido". **Resolvido: a introspecção fecha a
  questão.** O enum é `('none','admin','master')`; **`comum` não existe**. Logo
  `set-access/route.ts:58` (`admin_level: new_admin_level ?? "admin"`) escreve um valor
  **válido**, e o bug latente 🟠 do R-002 item 3 **não procede**. Quem está errado é
  `CONTEXT.md` §4.2 (congelado) e, por herança, a §1 da matriz.
- **Impacto:** nenhum em runtime. Mas a matriz é a lei do projeto, e uma célula errada nela
  vira policy errada na próxima migration — que é exatamente como se chega em SEC-004.
- **Como corrigir:** trocar `comum` por `admin` na §1 da matriz e fechar o item 3 do R-002 em
  `04-RISCOS.md`. É mudança de documento e, pela regra do meu papel, **paro e aviso** em vez de
  editar: a matriz só muda por decisão registrada.

---

## Verificado e OK

O que checei e está correto. Vale tanto quanto a lista de cima, e vários destes eram os pontos
onde a migration mais poderia ter errado.

- **`status` é inescrevível pelo próprio usuário.** O `alter policy` da 7.2 fixa a coluna com
  o mesmo padrão de subquery já validado em produção para `role`/`admin_level`/`admin_team`. Os
  quatro caminhos alternativos (policies, funções da seção 8, trigger de signup e as três rotas
  de API) foram percorridos e nenhum escreve `status` a mando do usuário. **Resposta A, na
  coluna `status`: fechado.**
- **A remoção da `profiles_update_own_safe` está certa e é obrigatória.** Provado nos dois
  sentidos na resposta D: subconjunto próprio antes da 7.2, e furo aberto depois dela se
  ficasse. Os dois `USING` são idênticos, o que sustenta a conclusão.
- **Nenhuma recursão de RLS.** As três funções usadas em policy são `SECURITY DEFINER` +
  `SET search_path = public`. A subquery da 7.2 dispara policies de SELECT, e nenhuma delas
  consulta `profiles` em modo INVOKER.
- **`current_user_role()` removida** (`:502`). Era a arma carregada do DL-014, `SECURITY
  INVOKER` e sem `search_path`, consultando `profiles`. Sai do banco. Fecha metade do R-005.
- **`is_admin_master()` removida** (`:503`), duplicata byte a byte da canônica. Fecha a outra
  metade do R-005.
- **`set_updated_at()` endurecida** com `search_path` (`:90-98`). Não é usada em policy e não
  consulta tabela, então `INVOKER` aqui é aceitável.
- **RLS ativa nas cinco tabelas novas** (`:234-238`). Nenhuma tabela nova fica sem RLS.
- **`animais` é o isolamento mais limpo do arquivo:** quatro policies, todas
  `tutor_id = auth.uid()`, e nem admin lê. Dado de saúde de animal de estimação é o tipo de
  coisa que não deveria estar ao alcance de operador, e não está.
- **`audit_logs`: leitura só por master, e nenhuma policy de escrita.** Correto nas duas
  pontas. `admin_definir_status` é `DEFINER` e roda como dono da tabela, então o `insert` passa
  por cima da RLS como pretendido, e ninguém forja nem apaga linha pelo cliente.
- **`contatos` sem policy de INSERT: intencional e correto** (DL-047). Falha fechada para
  `anon` e `authenticated`; `service_role` grava. O check constraint `contatos_tem_origem`
  (`:198`) garante que toda linha tenha `user_id` **ou** `anon_id`, o que impede contato órfão
  e sustenta a métrica que se vende ao profissional.
- **`contatos` identifica o anônimo por `anon_id` e não por IP.** O DL-047 argumenta o porquê e
  o schema obedece: não há coluna de IP em lugar nenhum. Ponto positivo de LGPD, e raro de ver
  feito por escolha.
- **`pending_validation` não aparece em leitura pública.** Conferido contra as duas policies
  `*_select_publico`: `perfil_esta_ativo` é falso e a linha some. **Resposta C, no eixo de
  linha: correto.**
- **A visibilidade da busca está no Postgres, não no Next.** É policy, não `.filter()` de
  cliente. A regra está incompleta (SEC-001), mas está no lugar certo, que é o que a matriz §3
  regra 2 exige e o que seria caro de corrigir depois.
- **`handle_new_user` continua recusando `admin` vindo do metadata** (`:452-455`), mantendo a
  garantia da 0001. `role` só é concedido no banco.
- **Superfície mínima nas funções da seção 8** (`:432-435`): `EXECUTE` revogado de `anon` e
  PUBLIC, concedido só a `authenticated`. É a prática certa — só não foi aplicada a
  `perfil_esta_ativo` e `is_admin` (SEC-009).
- **`admin_definir_status` recusa alvo cujo `role` não seja `vet`/`clinic`** (`:378-380`).
  Correto em intenção; vira problema só porque a SEC-001 cria perfis profissionais em contas
  `tutor`, que a função então não alcança.
- **Reprovar devolve para `incomplete`, e `concluir_onboarding_profissional` aceita rodar de
  novo** a partir de `incomplete`. O ciclo reprova → corrige → reenvia fecha, como a §4
  desenha. (Falta só o motivo chegar ao profissional — SEC-007.)
- **Índices coerentes com as consultas previstas** (`:515-524`): `(role, status)` cobre a fila
  de validação e o filtro da busca, GIN em `especialidades`/`servicos` cobre o filtro por
  especialidade, e os índices parciais de `contatos` em `user_id`/`anon_id` cobrem a vinculação
  do DL-047 passo 4 sem inchar o índice com nulos.
- **A migration é uma transação única** e nada tem `if not exists`. Uma reexecução acidental
  falha inteira em vez de aplicar pela metade.
- **Procedimento de reversão (seção 13) está escrito, é coerente e é honesto** sobre o que
  **não** restaura (`onboarding_completed` dos 6). Recria a `_safe` **e** restaura o
  `WITH CHECK` da `_safe_fields` sem `status`, mantendo o par consistente — que era o erro
  fácil de cometer ali.
- **Ordem de execução e sintaxe:** revisadas linha a linha, sem erro encontrado. Detalhado na
  resposta G.
- **Nenhum segredo no arquivo.** Nenhuma chave, nenhum `NEXT_PUBLIC_` indevido, nenhum
  `SUPABASE_SERVICE_ROLE_KEY` fora de `app/api/admin/*` com `runtime = "nodejs"`.
- **`docs/06-PERMISSOES.md` §7 (R-011)** continua sendo pergunta de produto, não de código.
  Fora do escopo desta revisão, sem mudança.

---

## Não consegui verificar

Tudo aqui exige acesso ao dashboard do Supabase ou a produção. Nenhum item bloqueia sozinho,
mas os dois primeiros valem 30 segundos cada **antes** de o Elber colar o SQL.

1. **Assinatura real de `admin_set_user_access` e `admin_list_profiles`.** É o único modo de
   falha total do arquivo (SEC-010). Query pronta no achado.
2. **Se as tabelas novas recebem `GRANT` para `anon`/`authenticated`.** No Supabase isso vem do
   `ALTER DEFAULT PRIVILEGES` do papel `postgres` e normalmente é automático quando o SQL roda
   pelo SQL Editor. Se não vier, as policies ficam corretas e a aplicação inteira devolve
   `42501`. **Falha fechada**, portanto não é risco de segurança — mas é um susto na janela de
   manutenção. Conferir depois com
   `select grantee, privilege_type from information_schema.role_table_grants where table_name='vet_profiles';`
3. **Se `FORCE ROW LEVEL SECURITY` continua desligado em `profiles`.** A baseline afirma que
   sim e a baseline é confiável; registro aqui porque é a condição de que a resposta B depende
   (SEC-011).
4. **Quem é o dono (`OWNER`) das funções `DEFINER` e das tabelas novas.** Toda a análise de
   não-recursão e de escrita em `audit_logs` assume dono = `postgres` = dono das tabelas, que é
   o padrão do SQL Editor. Se a migration for aplicada por outro papel, reavaliar.
5. **Como a tela da F3/S4 vai assinar a URL do documento.** A SEC-003 assume
   `createSignedUrl(documento_path)` com `service_role`, que é o único caminho possível com
   bucket privado, mas a tela ainda é casca e o código não existe. A correção da SEC-003 deve
   entrar **antes** dessa tela, não depois.
6. **Policies do bucket `documentos`** (T-002). Não existem ainda. A SEC-003 muda de 🟠 para 🔴
   se o bucket for criado com leitura pública ou com policy que não amarre o prefixo do caminho
   ao uid.
7. **Varredura do histórico do git por segredo.** Fora do escopo desta revisão pontual, que é
   pré-migration. Entra na T-004 (linha de base).

---

## VEREDITO

# NÃO.

**Esta migration não pode ser aplicada em produção hoje.**

Ela acerta o alvo declarado do card — `status` fica fora do alcance do usuário, toda função de
policy é `DEFINER` com `search_path`, não há recursão, a reversão está escrita e a remoção da
policy antiga é a decisão certa pelo motivo certo. O trabalho é bom. Mas ela erra o
**objetivo** que o alvo servia, e erra em dois pontos que custam exatamente o que a §3 da
matriz diz que não pode ser perdido.

O ponto que resume tudo: **a migration protege a coluna e deixa a porta ao lado aberta.**
Trancar `status` só vale se `status` for o único caminho para aparecer na busca. Não é —
`perfil_esta_ativo()` esqueceu o `role`, e o `INSERT` esqueceu o `role` junto. E proteger o
telefone com um evento de servidor só vale se o telefone não estiver na mesma linha que o
`anon` já pode ler inteira.

### O que precisa mudar antes de rodar — bloqueantes

1. **SEC-001** — `perfil_esta_ativo()` passa a exigir `role IN ('vet','clinic')`, e os dois
   `*_insert_own` passam a exigir que o dono tenha o role da tabela (via função `DEFINER`). Os
   dois lados: fechar só um deixa o outro contornável.
2. **SEC-002** — `REVOKE SELECT` da tabela para `anon`/`authenticated` e `GRANT SELECT` só nas
   colunas públicas, em `vet_profiles` e `clinic_profiles`. `whatsapp`, `telefone`,
   `email_contato` e `documento_path` **ficam de fora**. Alternativa equivalente: view pública
   com a tabela-base fechada.
3. **SEC-003** — `documento_path` sai da escrita do cliente: privilégio de coluna ou trigger
   que amarre o caminho ao prefixo `auth.uid()`.
4. **SEC-004** — `profiles_select_admin` recortada com `and role in ('vet','clinic')`.
5. **SEC-005** — o guard de master em `admin_definir_status` passa a testar também
   `status_antigo = 'suspended'` (exige mover a leitura do status para antes do guard).
6. **SEC-006** — policies de `UPDATE` de admin em `vet_profiles`/`clinic_profiles`, e uma
   decisão consciente sobre remoção de conteúdo (policy de `DELETE` para master, ou coluna
   `oculto` respeitada pelas policies públicas).

### Entram junto porque o custo marginal é praticamente zero com o banco já aberto

7. **SEC-007** — coluna `status_motivo` em `profiles`, escrita só por `admin_definir_status`,
   fixada na 7.2, legível pelo dono. Sem ela a S4 não entrega a reprova que a §4 desenha.
8. **SEC-009** — `revoke execute` de `anon`/PUBLIC em `perfil_esta_ativo(uuid)` e `is_admin()`.
9. **SEC-010** — conferir a assinatura real das duas RPC antes de colar (é o único modo de
   falha total do arquivo).
10. **SEC-008** e **SEC-012** — `slug` fora da escrita do cliente, e a 10.2 restrita ao alvo
    real.

### Podem esperar

**SEC-011** (comentário e item de checklist) e **SEC-013** (correção de documentação — e essa
eu **não** faço: a matriz só muda por decisão registrada, então fica para o Elber ou para o
`vetria-escriba`).

**Estimativa:** os seis bloqueantes são acréscimos locais dentro do próprio
`0002_nucleo.sql`, na faixa de 60 a 80 linhas. Nenhum exige repensar o desenho — o desenho está
certo. **Nova revisão obrigatória antes de aplicar**, e ela é rápida, porque o escopo já está
delimitado por este relatório.

---

## HANDOFF — vetria-seguranca — T-001 — 26/08/2026

**Fiz:** revisão pré-aplicação completa da `supabase/migrations/0002_nucleo.sql`, linha a
linha, contra `docs/06-PERMISSOES.md` §§1-6, `0000_baseline.sql`, DL-044 a DL-047,
`middleware.ts`, `lib/auth/painel.ts` e as três rotas de `app/api/*`. 13 achados: 2 🔴, 4 🟠,
6 🟡, 1 ⚪. Relatório em `docs/relatorios/SEC-2026-08-26.md`.

**Não fiz:** não toquei em nenhum `.sql`, `.ts` ou `.tsx` — não corrijo, reporto. Não editei
`docs/06-PERMISSOES.md` apesar de ter encontrado uma célula errada nela (SEC-013): a matriz só
muda por decisão registrada em `05-DECISOES.md`. Não varri o histórico do git por segredo (é a
T-004, e esta revisão era pré-migration).

**Estado agora:** a 0002 está **reprovada para aplicação**. Ela fecha corretamente a escrita de
`status` pelo usuário (o alvo do card) e não tem recursão de RLS, mas deixa dois furos 🔴 que
anulam o efeito prático disso: qualquer `tutor` publica perfil profissional na busca (SEC-001)
e o `anon` lê telefone, WhatsApp, email e `documento_path` de todo profissional ativo em uma
chamada (SEC-002). Os seis bloqueantes cabem em 60-80 linhas dentro do próprio arquivo, sem
mudar o desenho.

**Descobri:** (a) o `status` protege a **coluna**, não o **efeito** — a busca é alcançável sem
tocar nele, porque `perfil_esta_ativo()` e os dois `*_insert_own` esqueceram o `role`; (b)
**RLS é row-level e este projeto precisa de column-level** — três achados (SEC-002, SEC-003,
SEC-008) são a mesma lição, e ela vai se repetir em toda tabela com coluna pública e coluna
privada na mesma linha; (c) o enum `admin_level` é `('none','admin','master')`, não tem
`comum`, o que **fecha o R-002 item 3 como improcedente** e torna errada a §1 da matriz; (d) o
corte admin/master do DL-045 vazou em duas direções opostas de uma vez — admin comum lê demais
(SEC-004) e desfaz decisão de master (SEC-005), enquanto não consegue moderar nada (SEC-006).

**Bloqueios:** T-001 não aplica até os seis bloqueantes entrarem. T-002 (bucket) depende da
T-001 **e** da SEC-003, porque a policy do bucket assume um `documento_path` confiável e hoje
ele não é. F3/S4 (validação pelo admin) depende de SEC-006 e SEC-007, sem os quais admin não
modera e o reprovado não sabe o motivo. F4/S8 (evento de contato) depende de SEC-002, sem a
qual o DL-047 inteiro é decorativo.

**Próximo passo óbvio:** `vetria-backend` aplica os seis bloqueantes no próprio
`0002_nucleo.sql` (não uma 0003 — a 0002 ainda não foi aplicada, então corrigir na origem é
mais barato e não deixa dívida). Depois, nova revisão de segurança com escopo restrito aos
achados deste relatório. Só então a sessão presencial de aplicação, com backup confirmado.

**Docs que atualizei:** `docs/relatorios/SEC-2026-08-26.md` (novo). Nada mais — não tenho
permissão de escrita fora dele.

**Pendente para o `vetria-escriba`:** SEC-001 a SEC-006 viram T-005 a T-010 em
`03-TAREFAS.md`; SEC-013 corrige a §1 de `06-PERMISSOES.md` e fecha o item 3 do R-002 em
`04-RISCOS.md`; o card da T-001 ganha "revisado por `vetria-seguranca` em 26/08/2026 —
**reprovado**, ver SEC-2026-08-26".

**Commits:** nenhum. Auditor não commita código; o relatório fica para o Elber ou o escriba
versionar.

---
---

# SEGUNDA REVISÃO — 26/08/2026

**Escopo:** releitura integral, do zero, de `supabase/migrations/0002_nucleo.sql` (738 linhas)
após a correção dos achados da primeira revisão. **Base:** commit `6c34fd9`
(`fix(db): corrige a 0002 apos reprovacao na auditoria de seguranca`).
**Estado da migration:** **continua não aplicada.**

> A primeira revisão, acima, fica inteira e sem edição. Ter reprovado importa: é o registro de
> que os dois 🔴 existiram, e é o que permite conferir se morreram de verdade.

## Resumo da segunda revisão

**Os dois 🔴 morreram, e morreram pelo motivo certo, não por acidente.** A SEC-001 foi fechada
nas duas metades (função e policies de escrita), e a SEC-002 foi resolvida da única forma que
resolve de verdade: **separando a linha**, não tentando fazer RLS ter uma granularidade de
coluna que ela não tem. `perfil_privado` está de fato fechada, inclusive por embed do
PostgREST. Os quatro 🟠 da v1 estão fechados ou reduzidos.

**Mas sobram quatro 🟠 novos, e três deles nasceram das próprias correções.** O mais grave é
que a correção da SEC-009 (revogar `EXECUTE` das funções de policy) **desliga a busca pública
para o visitante anônimo**, que é o ator de primeira classe do DL-044. Além dele: o CHECK do
`documento_path` não cobre `..`, e o "deputado confuso" da SEC-003 volta por travessia de
caminho; o dono continua podendo trocar o CRMV **depois** de aprovado; e o bloco de reversão
**não roda** — ele apaga colunas que uma policy ainda referencia.

**Continua sem poder ser aplicada.** Mas a distância agora é de ~15 linhas e um reordenamento,
não de redesenho.

---

## 1. As correções fecham de fato, ou só mudam de lugar?

Fui atrás de cada uma. Veredito por achado da v1:

| Achado v1 | Status | Verificação |
|---|---|---|
| **SEC-001** 🔴 | ✅ **fechado** | `perfil_esta_ativo(p_id, p_role)` (`:106-114`) exige `role = p_role AND status='active'` — as duas metades da matriz §3 regra 2. E `tem_role()` guarda os quatro INSERT/UPDATE (`:330`, `:336`, `:361`, `:365`). Refiz o caminho antigo: um `tutor` (que nasce `active`) agora reprova no `WITH CHECK` do INSERT antes de chegar à leitura pública e, mesmo que a linha existisse por outro meio, `perfil_esta_ativo(id,'vet')` seria falso. **Fechado nos dois lados, que era o requisito.** |
| **SEC-002** 🔴 | ✅ **fechado** | `whatsapp`, `telefone`, `email_contato` e `documento_path` saíram para `perfil_privado` (`:220-231`). Detalhe que importa: **fechar era mesmo mudar de tabela.** RLS não tem granularidade de coluna, então resolver por policy teria sido teatro. A alternativa que eu sugeri (privilégio de coluna) funcionaria, mas a escolhida é mais robusta, porque não depende de alguém lembrar de refazer o `GRANT` ao acrescentar coluna nova. **Escolha melhor que a minha recomendação.** |
| **SEC-003** 🟠 | ⚠️ **parcial** | O CHECK existe (`:229-230`) e fecha o caso simples. **Não cobre `..`** — ver **SEC-015**. |
| **SEC-004** 🟠 | ✅ **fechado** | `:427-429` — `is_admin() and role in ('vet','clinic')`. Admin comum vê a fila, não a base. O master continua coberto por `profiles_select_all_master` da 0000. |
| **SEC-005** 🟠 | ✅ **fechado** | `:483-496` — a leitura da linha foi movida para **antes** do guard, e o guard testa `novo_status = 'suspended' or status_antigo = 'suspended'`. Confirmei que `status_antigo` já está populado quando o `if` roda. Admin comum não reativa mais. |
| **SEC-006** 🟠 | ✅ **fechado, e melhor do que eu pedi** | `vet_profiles_update_admin` e `clinic_profiles_update_admin` (`:344-346`, `:371-373`). E o delistamento, que era a outra metade da minha preocupação, funciona sem policy de DELETE: admin comum chama `admin_definir_status(x,'incomplete', motivo)`, o status deixa de ser `active`, `perfil_esta_ativo` vira falso e o perfil some da busca. **Confirmei que esse caminho não exige master** — o guard só trava `suspended`. DoD da F3/S4 fecha. |
| **SEC-007** 🟠 | ✅ **fechado** | `profiles.status_motivo` (`:150-151`), escrito por `admin_definir_status` (`:503`), pinado no 7.2 com `is not distinct from` (`:454`) — tratamento de NULL correto — e legível pelo dono via `profiles_select_own`, que já existia. O reprovado enxerga o motivo. |
| **SEC-008** 🟡 | ⚠️ **parcial** | O pin no UPDATE está correto. **O INSERT ficou livre** — ver **SEC-018**. |
| **SEC-009** 🟡 | ⚠️ **fechado com dano colateral** | A revogação funciona, mas derruba a busca pública — ver **SEC-014**. |
| **SEC-010** 🟡 | ✅ **fechado** | O bloco `do` anônimo (`:635-646`) descobre a assinatura por `oid::regprocedure` e simplesmente não itera se a função não existir. O único modo de falha total do arquivo desapareceu. |
| **SEC-011** 🟡 | ⬜ aberto (era para ficar) | Comentário e item de checklist. E agora vale também para `vet_profiles`/`clinic_profiles`, que ganharam policy consultando a própria tabela. |
| **SEC-012** 🟡 | ✅ **fechado no que importa** | `:612-616` — o UPDATE só toca quem não tem linha de perfil. Na primeira execução as tabelas estão vazias, então o efeito é idêntico ao da v1; numa reexecução, quem já preencheu de verdade é poupado. Falta só o banner de "já aplicada" no cabeçalho, depois de aplicada. |
| **SEC-013** ⚪ | ⬜ aberto | Correção de documentação (`06-PERMISSOES.md` §1: `comum` → `admin`). Não é minha para fazer. |

### `tem_role` reintroduz recursão ou vazamento? Não, nem uma coisa nem outra.

Foi a primeira coisa que fui verificar, porque a pergunta é legítima: é uma função que consulta
`profiles` e é chamada de dentro de policies de **outra** tabela.

**Recursão: não.** `tem_role` é `security definer set search_path = public` (`:118-126`). O
corpo roda como o dono da função, que é o dono de `profiles`, e com `FORCE ROW LEVEL SECURITY`
desligado a RLS de `profiles` não é reaplicada lá dentro. Mesmo que fosse, o laço não fecharia:
as policies de `profiles` não consultam `vet_profiles`. **A cadeia é `vet_profiles` → função
DEFINER → `profiles` → fim.** Um salto, não um ciclo.

**Vazamento: não, e por um motivo estrutural que vale registrar.** `tem_role(p_role)` só
responde **sobre `auth.uid()`**, isto é, sobre quem chamou. Ela não aceita um `id` de alvo.
Então, mesmo executável por RPC, ela não diz nada que o chamador já não saiba sobre si mesmo.
Isso a diferencia de `perfil_esta_ativo(p_id, p_role)`, que **aceita um uuid arbitrário** e por
isso é oráculo — foi o que motivou a SEC-009. **A assimetria está certa:** o perigo nunca foi
"função DEFINER em policy", foi "função DEFINER que responde sobre terceiros".

**Um caminho novo, que a v1 não tinha:** `vet_profiles_update_own` (`:334-340`) agora tem uma
subquery **sobre `vet_profiles`** dentro de uma policy **de `vet_profiles`**. É a mesma forma
do 7.2, e termina pelo mesmo motivo: a subquery dispara as policies de **SELECT** de
`vet_profiles`, que são `_select_publico` (chama `perfil_esta_ativo`, DEFINER), `_select_own`
(expressão pura) e `_select_admin` (chama `is_admin()`, DEFINER). Nenhuma volta para
`vet_profiles`. **Termina. ✅** Mas a armadilha da SEC-011 agora tem três tabelas em vez de
uma, e isso precisa estar no comentário.

**Custo escondido — não é segurança, mas é o mesmo código:** `perfil_esta_ativo` é `SECURITY
DEFINER`, e **função `SECURITY DEFINER` nunca é inlined pelo planejador**. Numa busca que varre
`vet_profiles`, ela vira uma chamada de função por linha, cada uma com um lookup na PK de
`profiles`. Na escala de hoje é irrelevante; quando a busca da F4 tiver filtro de cidade mais
especialidade sobre alguns milhares de linhas, vira o gargalo, e o remédio (uma coluna `visivel
boolean` mantida por trigger) é caro de retrofitar depois. Registro como nota, não como achado.

---

## 2. `perfil_privado` está mesmo fechada?

**Sim, por quatro camadas independentes.** Fui atrás dos três caminhos que a pergunta cita, e
de mais um.

1. **RLS ativa:** `:307`. ✅
2. **Nenhuma policy alcança `anon`.** As quatro policies (`:377-388`) são todas
   `to authenticated`. Confirmei varrendo o arquivo inteiro: as **únicas** duas ocorrências de
   `to anon` são `vet_profiles_select_publico` (`:316`) e `clinic_profiles_select_publico`
   (`:350`). Tabela com RLS ativa e zero policies aplicáveis ao papel = zero linhas, sempre. ✅
3. **Privilégio revogado:** `revoke all on public.perfil_privado from anon` (`:665`) tira o
   `GRANT ALL` que a default privilege do Supabase deu no `CREATE TABLE`. Ordem correta: a
   tabela nasce em `:220`, o revoke roda em `:665`, na mesma transação. ✅
4. **Embed do PostgREST:** `?select=*,perfil_privado(*)` **não vaza**, por dois motivos
   somados. (a) O embed do PostgREST se resolve por chave estrangeira, e **não existe FK entre
   `vet_profiles` e `perfil_privado`** — as duas apontam para `profiles`, não uma para a outra.
   (b) Mesmo que o PostgREST resolvesse a relação por `profiles`, **o embed é executado como o
   mesmo papel da requisição, e a RLS da tabela embutida é aplicada normalmente**: para `anon`
   não há policy, e o privilégio nem existe mais. Um join não é porta lateral para fora da RLS
   — é o mesmo `SELECT`, sujeito às mesmas policies. ✅

**Testei também o caminho de um usuário logado**, que é o menos óbvio: um `tutor` autenticado
fazendo `GET /rest/v1/vet_profiles?select=*,perfil_privado(*)` passaria por
`perfil_privado_select_own` (`id = auth.uid()`) e traria **só a própria linha**, nunca a do
profissional. ✅

**A conclusão que interessa:** o DL-047 volta a ter sentido. O telefone agora só existe atrás
do `service_role`, que é exatamente o que o evento de contato de servidor pressupõe. **SEC-002
fechada de verdade.**

**Uma sobra, que é achado próprio:** `clinic_profiles` continua com `cnpj`, `razao_social`,
`responsavel_tecnico`, `endereco` e `cep` do lado público. Ver **SEC-020**.

---

## 3. `slug is not distinct from` funciona como pin quando o valor é NULL?

**Sim, funciona exatamente como pretendido — e sim, ele impede o dono de definir o slug pela
primeira vez, que é o comportamento desejado.** Verifiquei os quatro casos:

| slug atual | slug enviado | `is not distinct from` | Resultado |
|---|---|---|---|
| `NULL` (estado inicial) | `NULL` (não mexeu) | **true** | ✅ o dono edita bio, cidade, CRMV normalmente |
| `NULL` | `'dr-fulano'` | **false** | 🔒 o dono **não** define o próprio slug |
| `'dr-fulano'` | `'dr-fulano'` | **true** | ✅ o dono edita o resto |
| `'dr-fulano'` | `'clinica-alvo'` | **false** | 🔒 sem squatting por troca |

É por isso que `is not distinct from` é o operador certo aqui e `=` seria o errado: com `=`, o
caso 1 — o mais comum, porque **todo perfil nasce com slug NULL** — resolveria para `NULL`, o
`WITH CHECK` trataria como falha, e **nenhum profissional conseguiria editar o próprio perfil**.
Que é exatamente o bug do DL-014 e da `profiles_update_own_safe` se repetindo em outra tabela.
A v2 acertou o operador, e acertou por entender o motivo, não por sorte.

**Quem define o slug, então?** Duas portas, ambas existem e ambas funcionam:

- o **servidor** na F4/S5, com `service_role`, que ignora RLS por completo; ✅
- o **admin**, por `vet_profiles_update_admin` (`:344-346`), cujo `WITH CHECK` é só
  `is_admin()` e portanto não carrega o pin. ✅ Isso também dá o caminho de recuperação de um
  slug tomado indevidamente, que na v1 não existia.

**A subquery lê o valor antigo?** Sim. Ela roda no snapshot do comando, então enxerga a linha
**antes** do UPDATE. É `NEW.slug` comparado com `OLD.slug`, que é o que um pin precisa ser. ✅

**Mas o pin só cobre metade da superfície** — o INSERT ficou de fora. Ver **SEC-018**.

---

## 4. A seção 11b revoga na ordem certa? Algum revoke quebra a F3 ou a F4?

**A ordem está certa. Dois revokes quebram funcionalidade prevista, e um deles é grave.**

Ordem: as tabelas são criadas na seção 4 (`:166-277`), as default privileges do Supabase se
aplicam no `CREATE TABLE`, e os revokes rodam em `:662-667`, depois. Dentro da mesma transação,
privilégio e policy são objetos independentes, sem ordem de dependência entre si. **Sem
problema de ordem.** ✅

Linha a linha:

| Linha | Revoke | Veredito |
|---|---|---|
| `:662-664` | `insert, update, delete` de `vet_profiles`/`clinic_profiles`/`animais`, de `anon` | ✅ correto. `anon` não tinha policy de escrita nessas tabelas, então é a segunda porta trancada, exatamente como o comentário promete. |
| `:665` | `all` de `perfil_privado`, de `anon` | ✅ correto e importante. É o que fecha o item 3 da resposta acima. |
| `:666` | `all` de `audit_logs`, de `anon, authenticated` | ⚠️ **contradiz a própria policy `audit_logs_select_master` criada em `:420-421`.** Ver **SEC-019**. |
| `:667` | `insert, update, delete` de `contatos`, de `anon, authenticated` | ✅ correto e alinhado ao DL-047. Confirmei que **não** quebra a F4/S8: tanto o INSERT do evento de contato quanto o `UPDATE ... WHERE anon_id` da vinculação passam por `service_role`, que ignora RLS e essas revogações. E `SELECT` foi preservado para `authenticated`, que é o que o profissional e o responsável precisam para ver o histórico. ✅ |

**E os revokes de função, em `:651-653`?** É aí que mora o problema sério.
`revoke execute on function public.perfil_esta_ativo(...) from anon, public` remove o `EXECUTE`
de `anon` **inclusive para o uso dentro da policy**, e `perfil_esta_ativo` é chamada por
`vet_profiles_select_publico` e `clinic_profiles_select_publico`, que são as **duas únicas
policies do arquivo que atendem `anon`**. Ver **SEC-014**, o achado principal desta segunda
revisão.

Os outros dois (`tem_role`, `is_admin`) **não** têm o mesmo problema: só são usados em policies
`to authenticated`, e o PostgreSQL só avalia as policies cujo papel casa com o papel da
consulta. `anon` nunca chega a avaliá-las. ✅

---

## 5. O CHECK do `documento_path` é contornável?

**Sim. Por travessia de caminho.** Ver **SEC-015**, com os passos.

Três coisas que **não** são problema, e que vale registrar porque fui atrás delas:

- **Metacaractere de LIKE no prefixo.** `like id::text || '/%'` monta o padrão a partir de um
  uuid. `%` e `_` são metacaracteres de LIKE, e `_` casaria com qualquer caractere — mas a
  representação textual de um uuid contém **apenas** dígitos hexadecimais e hífens. Nenhum `%`,
  nenhum `_`. O padrão é, na prática, prefixo literal. ✅ Não dá para forjar um uuid que vire
  curinga.
- **Imutabilidade da expressão.** `uuid::text`, `||` e `like` são todos imutáveis, então o
  CHECK é válido e não vai ser recusado na criação da tabela. ✅
- **CHECK referenciando outra coluna da mesma linha.** É constraint de tabela, não de coluna, e
  constraint de tabela pode referenciar qualquer coluna da linha. ✅ Sintaxe correta.

O que ele **não** cobre é `..`, e isso basta para reabrir o buraco que ele foi escrito para
fechar.

---

## 6. `admin_definir_status` ainda recusa alvo que não seja vet/clinic. Ainda sobra caso sem saída?

**A recusa está certa agora, e por um motivo que mudou.** Na v1 eu apontei essa recusa como
agravante da SEC-001: existia um jeito de um `tutor` ter perfil profissional público, e a função
então não conseguia alcançá-lo. **Com a SEC-001 fechada, esse caso deixou de existir** —
`tem_role('vet')` impede a linha de nascer. A recusa passou de "trava sem saída" para "guarda
coerente". ✅

Testei o caso adjacente que continua possível, e que **a v2 resolve bem**: um `vet` com linha
em `vet_profiles` e `status='active'` é rebaixado para `tutor` pelo master via
`/api/admin/set-access` — que roda com `service_role` e não dispara cascade. A linha em
`vet_profiles` **sobrevive**. Ela continua pública? **Não:** `perfil_esta_ativo(id,'vet')` exige
`role = 'vet'`, o role agora é `tutor`, a função retorna falso e o perfil **some da busca
sozinho**. ✅ Rebaixamento virou seguro por construção, sem limpeza manual. Esse é um efeito
colateral bom da correção da SEC-001, e vale registrar porque ninguém o projetou de propósito.

**Sobra um caso sem saída, sim, e ele é mais de produto que de código:** **não existe forma de
suspender uma conta que não seja `vet` nem `clinic`.** Um `tutor` que abuse do evento de contato
— spam em massa contra profissionais, que é justamente a métrica vendida — não pode ser suspenso
por nenhuma função do produto, só por `service_role` na mão. A matriz §4 de fato define o portão
de status só para profissionais, então **não é divergência da matriz**: é uma lacuna da matriz
que a migration herda fielmente. Registro em **SEC-022**, junto com o problema irmão do
`onboarding_completed`, para virar decisão — não para bloquear.

---

## 7. Achados novos

### SEC-014 — O `revoke execute` da SEC-009 desliga a busca pública para o visitante anônimo · 🟠

- **Onde:** `0002_nucleo.sql:651` (`revoke execute on function public.perfil_esta_ativo(uuid,
  public.user_role) from anon, public`), contra `:315-317` e `:349-351` (as duas policies que
  chamam a função e são as únicas do arquivo que atendem `anon`).
- **O quê:** o PostgreSQL verifica o privilégio `EXECUTE` de uma função **contra o papel que
  está rodando a consulta**, e faz isso também quando a chamada está dentro de uma expressão de
  policy de RLS. Expressão de policy não é `SECURITY DEFINER`: ela é avaliada como o usuário da
  consulta. `SECURITY DEFINER` na função troca o usuário **dentro do corpo**, depois que a
  checagem de `EXECUTE` já passou — não antes. E `perfil_esta_ativo` é `language sql` +
  `security definer`, e **função `SECURITY DEFINER` nunca é inlined pelo planejador**, então
  ela é sempre uma chamada de função de verdade, com checagem de privilégio de verdade.

  Resultado: `anon` perde o `EXECUTE` (o revoke nomeia `anon` **e** `public`, então não sobra
  caminho) e passa a bater em `permission denied for function perfil_esta_ativo` **em toda
  consulta a `vet_profiles` e `clinic_profiles`**.

  A ironia vale registrar: **a correção de um achado 🟡 desligou o produto.** O oráculo da
  SEC-009 era real, mas a função existe para duas coisas — ser chamada pela policy e ser
  chamável por RPC — e o revoke, que só queria matar a segunda, matou as duas.
- **Como explorar:** não é ataque, é o caminho feliz quebrando. Sem sessão nenhuma:
  `GET /rest/v1/vet_profiles?select=nome_exibicao,cidade` com a chave anônima retorna erro
  `42501` em vez da lista. **Toda a busca da F4 e todo perfil público
  (`/veterinario/[slug]`, `/estabelecimento/[slug]`) ficam quebrados para quem não está
  logado.** Falha fechada — não vaza nada — mas quebra tudo.
- **Impacto:** contradiz o DL-044, que fez do visitante anônimo ator de primeira classe, e a §2
  da matriz, que dá `✅` a visitante em `/`, `/buscar` e nos dois perfis públicos. Bloqueia a
  F4 inteira. E o modo de falha é o pior possível: **em desenvolvimento ninguém percebe**,
  porque o dev está sempre logado e `authenticated` mantém o `EXECUTE` pela default privilege
  do Supabase. O bug só aparece na janela anônima, ou em produção, no funil de entrada.
- **Como corrigir:** não revogar `EXECUTE` de uma função que uma policy `to anon` chama. Para
  matar o oráculo sem matar a policy, mover `perfil_esta_ativo` para um schema que o PostgREST
  não expõe (um `private`, com `grant usage` para `anon` e `authenticated`): o PostgREST só
  publica RPC do schema exposto, então a função some da API mas continua chamável pela policy.
  Se isso for muito para agora, a alternativa honesta é **desfazer só a linha `:651`** e aceitar
  de volta o 🟡 da SEC-009, que era oráculo de confirmação e nunca foi dump. As linhas `:652` e
  `:653` (`tem_role`, `is_admin`) podem ficar: são usadas só em policies `to authenticated`, e
  `anon` nunca as avalia.
- **Verificar antes:** este achado também mede o raio do estrago. Rodar
  `select has_function_privilege('anon','public.perfil_esta_ativo(uuid,public.user_role)','execute'), has_function_privilege('authenticated','public.perfil_esta_ativo(uuid,public.user_role)','execute');`
  depois de aplicar num ambiente de teste. Se `authenticated` também vier `false` (ou seja, se
  este projeto não tiver `alter default privileges ... on functions` para os papéis do
  Supabase), a busca quebra para **todo mundo**, não só para o anônimo.
- **Vira task:** T-011

### SEC-015 — O CHECK do `documento_path` não cobre `..`, e o deputado confuso da SEC-003 volta · 🟠

- **Onde:** `0002_nucleo.sql:229-230`
  (`check (documento_path is null or documento_path like id::text || '/%')`).
- **O quê:** o CHECK ancora o **prefixo** e nada mais. Ele aceita qualquer coisa depois da
  primeira barra, incluindo segmentos `..`. E `..` não é caractere proibido em chave de objeto
  do Storage — é texto comum. O problema é que, entre o `createSignedUrl()` e o storage-api,
  o caminho atravessa um **parser de URL**, e parser de URL normaliza `..`: pela especificação
  WHATWG, que é o que o `fetch` do cliente JS implementa, `/a/../b` é reescrito para `/b`
  **antes** de a requisição sair. O CHECK valida a string guardada; o que chega no Storage é a
  string normalizada. São coisas diferentes, e é na diferença que mora o buraco.
- **Como explorar:**
  1. Descobrir o uuid da vítima A. É trivial: `vet_profiles.id` **é** o uid, e a tabela é
     lida publicamente por qualquer um. `GET /rest/v1/vet_profiles?select=id`.
  2. Como o atacante B, autenticado e com `role='vet'`:
     `PATCH /rest/v1/perfil_privado?id=eq.<uid de B>` com
     `{"documento_path":"<uid de B>/../<uid de A>/documento.pdf"}`.
     **O CHECK aprova**, porque a string começa com `<uid de B>/`.
  3. B chama `concluir_onboarding_profissional()` e entra na fila.
  4. O admin abre a validação de B na F3/S4. A tela lê `documento_path` e chama
     `createSignedUrl(path)` com `service_role` — é o único caminho possível com bucket
     privado. O `..` é normalizado no trajeto e a URL assinada aponta para
     `<uid de A>/documento.pdf`: **o documento de identidade de A**.
- **Impacto:** idêntico ao da SEC-003 original, que este CHECK foi escrito para fechar — RG,
  CNH ou comprovante de CRMV de terceiro aberto pelo admin, e o acesso registrado em
  `audit_logs` como legítimo, o que envenena a trilha. A diferença é que agora existe uma
  defesa que **parece** cobrir o caso e não cobre, o que é pior que não ter defesa nenhuma:
  quem ler o schema vai concluir que o assunto está resolvido.
- **Como corrigir:** trocar o `like` por um regex que descreva o formato inteiro em vez de só
  o prefixo, por exemplo
  `check (documento_path is null or documento_path ~ ('^' || id::text || '/[A-Za-z0-9._-]{1,128}$'))`.
  Isso mata `..` (a barra fica fora da classe de caracteres, então não há segundo nível), mata
  subdiretório, mata caractere exótico e mata nome vazio, tudo numa linha. Se preferir manter o
  `like`, o mínimo é acrescentar `and documento_path not like '%..%'` — mas o regex é mais
  barato de auditar depois, porque descreve o que é permitido em vez de listar o que é proibido.
- **Vira task:** T-012

### SEC-016 — O profissional troca o CRMV depois de aprovado, e nada revalida · 🟠

- **Onde:** `0002_nucleo.sql:334-340` (`vet_profiles_update_own`) e `:363-369`
  (`clinic_profiles_update_own`). O `WITH CHECK` pina **só o `slug`**.
- **O quê:** a validação é um carimbo de um instante, e o dado carimbado continua editável
  depois. O DL-046 decidiu, com razão, que o profissional edita o perfil **enquanto espera** —
  mas a policy não distingue "enquanto espera" de "depois de aprovado". Um `vet` com
  `status='active'` continua com `UPDATE` livre sobre `crmv`, `crmv_uf` e `nome_exibicao`;
  um `clinic` idem sobre `cnpj`, `razao_social` e `responsavel_tecnico`.
- **Como explorar:**
  1. Cadastrar-se como `vet` com CRMV real e documento real. Aguardar. O admin aprova. Status
     vira `active` e o perfil entra na busca. Até aqui tudo legítimo.
  2. `PATCH /rest/v1/vet_profiles?id=eq.<meu uid>` com
     `{"crmv":"<CRMV de outra pessoa>","nome_exibicao":"Dr. Outro","especialidades":["cirurgia"]}`.
     A policy aprova: `id = auth.uid()`, `tem_role('vet')` verdadeiro, `slug` intocado.
  3. A mudança está **no ar imediatamente**, no perfil público indexável, sem passar por
     ninguém. O `status` continua `active` e nada reentra na fila.
- **Impacto:** é o mesmo dano da SEC-001, alcançado por outra porta e com um passo a mais de
  paciência. A promessa central da V1 é "profissional validado", e o que a plataforma valida é
  um estado que o próprio validado pode reescrever depois. Também apaga a rastreabilidade: o
  `audit_logs` registra a aprovação com o CRMV antigo, e não há registro nenhum da troca. Se um
  dia isso for parar em disputa, a plataforma não consegue provar o que aprovou.
- **Como corrigir:** duas direções, e a primeira é mais barata. (a) **Trigger** `BEFORE UPDATE`
  que, quando `status='active'` e algum campo validado mudou, devolve o perfil a
  `pending_validation` e registra em `audit_logs` — o profissional continua podendo corrigir,
  só volta para a fila, que é o comportamento correto e não bloqueia ninguém. (b) **Pinar os
  campos validados** no `WITH CHECK` com o mesmo `is not distinct from` já usado no `slug`,
  condicionado ao status. A (a) é melhor de produto: não trava a edição, só reabre a revisão.
- **Vira task:** T-013

### SEC-017 — O bloco de reversão não roda: ele apaga colunas que uma policy ainda referencia · 🟠

- **Onde:** `0002_nucleo.sql:707-708` contra `:727-735` (seção 13, procedimento de reversão).
- **O quê:** ordem invertida. O bloco faz, nesta sequência: `alter table public.profiles drop
  column if exists status_motivo`, depois `drop column if exists status`, e **só lá embaixo**
  restaura o `WITH CHECK` da `profiles_update_own_safe_fields` para a versão que não menciona
  essas colunas. Mas nesse momento a policy **ainda** referencia as duas (`:453-454`), e
  `CREATE POLICY` registra dependência em `pg_depend` sobre cada coluna citada na expressão.
  `ALTER TABLE ... DROP COLUMN` em coluna com dependente é `RESTRICT` por padrão: levanta
  `cannot drop column status of table profiles because other objects depend on it`. E se
  alguém, no aperto, acrescentar `CASCADE` para destravar, o `CASCADE` **derruba a policy
  inteira**, e aí a linha `:727` (`alter policy ...`) falha com `policy does not exist`. Os dois
  caminhos terminam em transação abortada.
- **Como explorar:** não é ataque. É a rede de segurança de uma migration 🔴 presencial, em
  produção, com backup obrigatório — descobrindo-se rota **no momento em que ela é acionada**,
  que é sempre o pior momento possível. Reproduzível colando a seção 13 depois de aplicar a
  0002 num ambiente de teste: ela para na linha `:707`.
- **Impacto:** se algo der errado depois do `commit`, não existe reversão. Sobra só o restore
  do backup, que é ordens de magnitude mais caro, mais lento e mais arriscado — e que o próprio
  cabeçalho da migration trata como último recurso, não como plano A.
- **Como corrigir:** inverter a ordem — primeiro `alter policy profiles_update_own_safe_fields`
  de volta para a versão sem `status`/`status_motivo`, **depois** derrubar as colunas. Vale
  conferir a mesma classe de problema no resto do bloco (as tabelas podem ser derrubadas em
  qualquer ordem porque o `drop table` leva junto as policies delas, e os `drop function` vêm
  depois de as policies que os usavam já terem sumido com as tabelas — esses dois estão certos).
- **Vira task:** T-014

### SEC-018 — SEC-008 fechou só metade: o `slug` continua livre no INSERT · 🟡

- **Onde:** `0002_nucleo.sql:328-330` (`vet_profiles_insert_own`) e `:359-361`
  (`clinic_profiles_insert_own`). O `WITH CHECK` é `id = auth.uid() and tem_role('vet')` — sem
  nenhuma menção a `slug`.
- **O quê:** o pin de `slug` foi aplicado ao `UPDATE` e esquecido no `INSERT`. Como o perfil é
  criado pelo próprio dono (é a policy que existe para isso), a primeira gravação da linha pode
  já trazer o `slug` escolhido a dedo.
- **Como explorar:** cadastrar-se pelo funil de veterinário (grátis, sem validação para chegar
  a `incomplete`), e como primeiro ato: `POST /rest/v1/vet_profiles` com
  `{"id":"<meu uid>","slug":"clinica-alvo"}`. O índice único crava o slug. Repetir com contas
  novas para tomar quantos slugs quiser.
- **Impacto:** menor que na v1, por duas razões concretas — agora exige uma conta com
  `role='vet'` por slug (o funil B2B, não o de responsável), e agora **é recuperável**, porque
  o `vet_profiles_update_admin` da correção da SEC-006 permite ao admin liberar o slug. Continua
  sendo squatting de identidade de URL, barato e roteirizável, contra um ativo de SEO que a F4
  vai passar a expor.
- **Como corrigir:** uma condição no `WITH CHECK` do INSERT: `and slug is null`. O slug passa a
  nascer nulo sempre, e só o servidor ou o admin o definem — que é exatamente a regra que o
  comentário da linha `:168` já declara e que o UPDATE já cumpre.

### SEC-019 — A seção 11b revoga de `authenticated` o privilégio de que a policy do master depende · 🟡

- **Onde:** `0002_nucleo.sql:666` (`revoke all on public.audit_logs from anon, authenticated`)
  contra `:420-421` (`create policy audit_logs_select_master ... for select to authenticated
  using (public.is_master_admin())`).
- **O quê:** a migration cria uma policy que dá leitura de `audit_logs` ao master **como
  `authenticated`** — porque master é `role='admin' + admin_level='master'`, e o papel de banco
  dele é `authenticated`, como o de todo mundo logado — e, 246 linhas depois, revoga `ALL` de
  `authenticated` nessa mesma tabela. Policy e privilégio se compõem por E: sem `SELECT`
  concedido, a policy nunca chega a ser avaliada. **A policy vira código morto no ato.**
- **Como explorar:** não é vazamento — é falha fechada. Logado como master:
  `GET /rest/v1/audit_logs?select=*` devolve `42501`. A célula "Ler `audit_logs` — Master ✅" da
  matriz §5 não se cumpre pelo caminho que a migration desenhou.
- **Impacto:** na prática o painel provavelmente vai funcionar assim mesmo, porque o padrão já
  estabelecido em `app/api/admin/*` é ler com `SERVICE_ROLE` (é o que `profiles/route.ts:64`
  faz), e `service_role` ignora tanto RLS quanto essa revogação. O dano real é de legibilidade
  do modelo de segurança: quem ler o schema em seis semanas vai ver a policy, concluir que o
  master lê `audit_logs` pela sessão dele, e escrever código que falha em silêncio. **Modelo que
  mente sobre si mesmo é dívida de segurança, mesmo quando o efeito líquido é seguro.**
- **Como corrigir:** escolher um dos dois e ser explícito. Ou manter a policy e revogar de
  `authenticated` só `insert, update, delete`, deixando o `select` para ela governar; ou manter
  o `revoke all` e **apagar a policy**, com um comentário dizendo que `audit_logs` é
  exclusivamente `service_role`. A primeira é mais coerente com o resto do arquivo, que põe a
  autorização nas policies.

### SEC-020 — `clinic_profiles` ainda publica CNPJ, razão social e o nome do responsável técnico para `anon` · 🟡

- **Onde:** `0002_nucleo.sql:192-209`, expostas por `clinic_profiles_select_publico` (`:349-351`).
- **O quê:** a separação da SEC-002 classificou corretamente telefone, WhatsApp, email e
  documento. Cinco colunas ficaram do lado público sem passar por essa triagem: `cnpj`,
  `razao_social`, `responsavel_tecnico`, `endereco` e `cep`. Só `site` recebeu comentário
  dizendo que é público de propósito (`:205`) — as outras cinco ficaram por omissão, não por
  decisão.
- **Como explorar:** com a chave anônima, sem sessão:
  `GET /rest/v1/clinic_profiles?select=nome_fantasia,razao_social,cnpj,responsavel_tecnico,endereco,cep&limit=10000`.
  Sai um cadastro completo de empresas mais o nome da pessoa física responsável por cada uma,
  em uma requisição.
- **Impacto:** menor que o da SEC-002, e parte dele é legítimo — endereço e cidade são o produto
  de uma vitrine de estabelecimento, e CNPJ é dado público na Receita. Os dois que merecem
  decisão consciente são **`responsavel_tecnico`**, que é o nome de uma pessoa física que pode
  não ser a titular da conta e não consentiu em virar dado público, e **`razao_social`**, que em
  MEI e firma individual carrega o nome civil do dono. Sob LGPD, "é público na Receita" não é o
  mesmo que "pode ser republicado em massa por terceiro com finalidade própria".
- **Como corrigir:** decidir coluna por coluna, agora que a tabela já está partida em duas e o
  custo de mover é uma linha. Meu palpite: `responsavel_tecnico` vai para `perfil_privado`;
  `cnpj`, `razao_social`, `endereco` e `cep` ficam públicos com registro explícito da decisão,
  no mesmo formato do comentário que `site` já tem. **A regra é o comentário, não a coluna:**
  toda coluna pública deve ter dito por escrito que quer ser pública.

### SEC-021 — `perfil_privado` aceita INSERT de qualquer role, ao contrário das outras duas tabelas · 🟡

- **Onde:** `0002_nucleo.sql:383-384` (`perfil_privado_insert_own`, `with check (id = auth.uid())`)
  contra `:328-330` e `:359-361`, que exigem `tem_role`.
- **O quê:** assimetria não declarada. As duas tabelas de perfil profissional ganharam guarda de
  role na correção da SEC-001; `perfil_privado`, que nasceu na mesma correção, ficou sem. Faz
  algum sentido — ela serve `vet` e `clinic` e uma checagem de role única seria desajeitada —
  mas o efeito é que um `tutor` ou um `admin` pode criar a própria linha lá.
- **Como explorar:** como `tutor` autenticado, `POST /rest/v1/perfil_privado` com
  `{"id":"<meu uid>","documento_path":"<meu uid>/rg.pdf","telefone":"..."}`. A linha entra. Só
  o próprio e o admin conseguem lê-la, então **não vaza nada**: é por isso que é 🟡 e não mais.
- **Impacto:** duas consequências pequenas e uma que vale mais. As pequenas: lixo na tabela, e
  uma linha de `perfil_privado` sem `vet_profiles`/`clinic_profiles` correspondente aparecendo
  para o admin. A que vale mais: quando a T-002 escrever as policies do bucket `documentos`, a
  pergunta "quem pode ter documento?" vai ter duas respostas diferentes dependendo de olhar para
  `vet_profiles` ou para `perfil_privado`. **Divergência de guarda entre tabelas irmãs é como
  furo nasce na migration seguinte.**
- **Como corrigir:** `with check (id = auth.uid() and (public.tem_role('vet') or
  public.tem_role('clinic')))`, e o mesmo no `USING`/`WITH CHECK` do UPDATE, para que a regra
  fique idêntica à das outras duas.

### SEC-022 — Reprovar deixa `status` e `onboarding_completed` em desacordo; e não há como suspender conta não-profissional · 🟡

- **Onde:** `0002_nucleo.sql:502-504` (`admin_definir_status` escreve `status` e `status_motivo`,
  **não** `onboarding_completed`) e `:543-547` (`concluir_onboarding_profissional` escreve os
  dois juntos). Mais `:498-500` (a recusa de alvo não-profissional).
- **O quê:** dois problemas irmãos, ambos de coerência de estado.
  1. Ao reprovar, o admin devolve o profissional a `status='incomplete'`, mas
     `onboarding_completed` **continua `true`**, porque só a função de conclusão o escreve. O
     usuário fica num estado que a §4 da matriz não prevê: `incomplete` mas "onboarding
     concluído". Se alguma tela rotear por `onboarding_completed` — e o código de hoje rota por
     ele — o reprovado é mandado para o painel em vez de para o onboarding, e fica sem lugar
     onde corrigir o que foi reprovado. O `status_motivo` da correção da SEC-007 fica visível
     numa tela que ele não alcança.
  2. `admin_definir_status` recusa alvo cujo `role` não seja `vet`/`clinic`, o que agora está
     **certo** (ver resposta 6), mas deixa o produto sem nenhum caminho para suspender uma conta
     de responsável — inclusive uma que esteja abusando do evento de contato do DL-047.
- **Como explorar:** (1) `admin_definir_status(x,'incomplete','CRMV ilegível')` e depois
  `select status, onboarding_completed from profiles where id = x` → `incomplete` / `true`.
  (2) `admin_definir_status('<uid de um tutor>','suspended')` → `status so se aplica a vet e
  clinic`, mesmo chamado por master.
- **Impacto:** (1) é bloqueio funcional do ciclo reprova → corrige → reenvia, que é o coração da
  F3/S4, e ele acontece **em silêncio**, na forma de um usuário perdido em vez de um erro.
  (2) é lacuna de moderação, herdada da matriz e não inventada aqui.
- **Como corrigir:** (1) `admin_definir_status` passa a escrever
  `onboarding_completed = (novo_status <> 'incomplete')` no mesmo `UPDATE`, e o roteamento das
  telas passa a olhar `status`, nunca `onboarding_completed` — a §4 da matriz é escrita em cima
  de `status`, e ter duas fontes de verdade para o mesmo gate é o que produz este bug.
  (2) é decisão de produto para o `05-DECISOES.md`, não conserto de SQL: ou a matriz §4 passa a
  prever suspensão de qualquer conta, ou fica registrado que moderação de responsável é manual
  na V1.

### Miudezas · ⚪

- **`:684`** — o índice `idx_profiles_status_motivo` indexa `status`, não `status_motivo`. Não
  quebra nada (o índice parcial da fila de validação é útil), mas o nome mente e vai confundir
  quem for otimizar a query da fila. Renomear para `idx_profiles_fila_validacao`.
- **`:667`** — `contatos` mantém `SELECT` concedido a `anon`. Sem efeito prático, porque não há
  policy que alcance `anon`, mas é a única tabela sensível onde 11b não seguiu o padrão de
  `perfil_privado`. Por simetria, `revoke select on public.contatos from anon`.
- **`:635-646`** — o bloco `do` anônimo não fixa `search_path`. `regprocedure::text` qualifica o
  schema conforme o `search_path` da sessão, então o resultado é correto em qualquer cenário
  plausível do SQL Editor; ainda assim, um `set search_path = public` no início do bloco tira a
  última dependência de ambiente do arquivo.
- **Seção 13** — a reversão não re-concede `EXECUTE` de `admin_list_profiles` e
  `admin_set_user_access` a `anon`. É deliberado e correto (aquilo era superfície desnecessária
  desde sempre), mas merece uma linha dizendo que a omissão é intencional, senão a próxima
  auditoria vai apontá-la como esquecimento.
- **`:520-549`** — `concluir_onboarding_profissional` continua sem verificar se existe linha em
  `vet_profiles`/`clinic_profiles` ou se há documento enviado. Um profissional pode entrar na
  fila com o cadastro vazio. Ruído operacional na F3/S4, não risco.

---

## 8. Verificado e OK na segunda revisão

Além do que a tabela da seção 1 já registra como fechado:

- **Sintaxe e ordem de execução, relidas do zero.** Nenhum erro. Em particular conferi:
  `create or replace` de `perfil_esta_ativo` com **assinatura nova** cria uma função nova em vez
  de substituir a antiga — o que seria armadilha se a v1 tivesse sido aplicada, e não foi, então
  não há sobra da versão de um argumento; o `revoke` de `:651` e o `drop` da reversão usam a
  assinatura de dois argumentos, coerentes entre si; `update public.profiles p set ...` com
  alias sem `AS` é sintaxe válida; o bloco `do` anônimo dentro de transação é permitido; o CHECK
  com `id::text` é imutável e válido; `status_motivo` (`:150`) nasce antes de o 7.2 referenciá-lo
  (`:454`); e as revogações de 11b vêm depois dos `create table`.
- **O 7.2 continua correto e agora pina cinco colunas.** `role`, `admin_level`, `admin_team`,
  `status` e `status_motivo`. O uso de `is not distinct from` no `status_motivo` (`:454`) e de
  `=` no `status` (`:453`) está certo em ambos os casos: `status` é `NOT NULL`, então `=` basta;
  `status_motivo` é nulável, então precisa de `is not distinct from`. **Alguém pensou coluna por
  coluna em vez de copiar o padrão.**
- **A remoção da `profiles_update_own_safe` (7.1) continua no lugar** e continua sendo
  pré-condição do 7.2 — agora ainda mais, porque o `WITH CHECK` dela também não menciona
  `status_motivo`.
- **`animais`** segue com o isolamento mais limpo do arquivo, inalterado.
- **`contatos`** segue sem policy de INSERT, agora reforçado por revogação de privilégio em
  `:667`. Cinto e suspensório, na direção certa.
- **`handle_new_user`** segue recusando `admin` vindo do metadata.
- **Nenhuma tabela nova sem RLS.** Seis tabelas, seis `enable row level security` (`:305-310`),
  incluindo a nova `perfil_privado`.
- **Nenhum segredo no arquivo**, e nenhum caminho novo por onde `anon` alcance `profiles`.
- **O rebaixamento de role passou a ser seguro por construção** (ver resposta 6). Não estava no
  pedido de correção e saiu de graça.

---

## 9. Não consegui verificar (segunda revisão)

1. **Se `authenticated` mantém `EXECUTE` em `perfil_esta_ativo` depois do `:651`.** Determina se
   a SEC-014 quebra a busca só para o anônimo ou para todo mundo. Query no achado. **É o
   primeiro teste a rodar.**
2. **Se o `fetch`/storage-api normaliza mesmo o `..` no caminho assinado.** A SEC-015 se apoia
   nisso. Vale um teste direto: `createSignedUrl('<uuid>/../<outro-uuid>/x.pdf')` num bucket de
   teste, e olhar qual objeto a URL devolve. **Se não normalizar, a SEC-015 cai para 🟡** — mas
   o CHECK continua devendo proibir `..`, porque a normalização é comportamento de biblioteca e
   pode mudar numa atualização sem ninguém notar.
3. **Se o bloco de reversão falha exatamente onde eu digo.** A SEC-017 é dedução a partir da
   dependência que `CREATE POLICY` registra sobre as colunas citadas. Confirmável em 30 segundos
   num ambiente de teste, colando a seção 13 depois de aplicar a 0002.
4. **`GRANT` das tabelas novas para `authenticated`** — mesmo item da primeira revisão, ainda
   não verificado, ainda falha fechada se der errado.
5. **`OWNER` das funções `DEFINER` e das tabelas.** Toda a análise de não-recursão assume dono =
   `postgres` = dono de `profiles`.
6. **Policies do bucket `documentos`** (T-002), que não existem. A SEC-015 sobe para 🔴 se o
   bucket for criado com leitura pública.

---

## VEREDITO DA SEGUNDA REVISÃO

# NÃO.

**Ainda não pode ser aplicada em produção hoje — mas a distância encolheu de redesenho para
lista de compras.**

O que mudou de categoria: **os dois 🔴 acabaram.** Não foram maquiados, foram resolvidos na
raiz. A SEC-001 foi fechada nas duas metades, e não em uma só; a SEC-002 foi resolvida partindo
a linha em duas tabelas, que é mais robusto que o privilégio de coluna que eu tinha sugerido,
porque não depende de ninguém lembrar de nada quando a próxima coluna nascer. `perfil_privado`
resistiu aos quatro caminhos que testei, embed do PostgREST incluído. Isso é trabalho bem feito,
e é justo dizer.

O que impede o `sim` são quatro 🟠, e o padrão entre eles é o que mais interessa: **três dos
quatro nasceram das correções.** Revogar `EXECUTE` para matar um oráculo 🟡 desligou a busca
pública. Um CHECK de prefixo fechou o caso óbvio do `documento_path` e deixou `..` aberto,
que é pior que não ter CHECK, porque agora existe uma defesa que parece cobrir. E o bloco de
reversão, que é a rede de segurança de uma migration presencial, apaga colunas que uma policy
ainda referencia e por isso **não roda**. Corrigir sob pressão é assim: o buraco novo aparece
onde a atenção acabou de sair.

### Bloqueantes — precisam entrar antes de rodar

1. **SEC-014** — não revogar `EXECUTE` de `perfil_esta_ativo` de `anon` (linha `:651`). Mover a
   função para um schema não exposto, ou desfazer a linha e aceitar de volta o 🟡 da SEC-009.
   **Rodar antes o teste de `has_function_privilege` do achado**, para saber se o estrago
   atinge também o usuário logado.
2. **SEC-015** — trocar o `like` de prefixo por regex de formato completo no CHECK do
   `documento_path`, fechando `..`.
3. **SEC-016** — trigger que devolve o perfil a `pending_validation` quando um campo validado
   (`crmv`, `crmv_uf`, `cnpj`, `razao_social`, `responsavel_tecnico`) muda com `status='active'`.
4. **SEC-017** — inverter a ordem na seção 13: restaurar o `WITH CHECK` da policy **antes** de
   derrubar `status` e `status_motivo`. Sem reversão que roda, a migration não tem rede.

### Entram junto, porque são uma linha cada e o banco vai estar aberto

5. **SEC-018** — `and slug is null` no `WITH CHECK` dos dois INSERT.
6. **SEC-019** — decidir entre manter a policy de `audit_logs` (e revogar de `authenticated` só
   `insert, update, delete`) ou apagá-la e declarar a tabela como `service_role`-only.
7. **SEC-021** — guarda de role no INSERT/UPDATE de `perfil_privado`, igual à das tabelas irmãs.
8. **SEC-022 (1)** — `admin_definir_status` passa a escrever `onboarding_completed` junto com o
   `status`, para que reprovar não deixe os dois em desacordo.
9. **⚪** — renomear `idx_profiles_status_motivo`; `revoke select on contatos from anon`;
   `set search_path` no bloco `do`.

### Viram decisão, não conserto

**SEC-020** (classificar as cinco colunas públicas de `clinic_profiles`, com comentário
explícito em cada uma), **SEC-022 (2)** (suspensão de conta não-profissional entra ou não na
matriz §4), **SEC-011** (comentário e item de checklist, agora cobrindo três tabelas) e
**SEC-013** (a §1 da matriz continua dizendo `comum`, e o enum continua sem esse valor).

**Estimativa:** os quatro bloqueantes são uma linha removida, um CHECK reescrito, um trigger de
oito linhas e um reordenamento de duas instruções. Os cinco seguintes são uma linha cada. **Nada
aqui exige repensar nada** — a v2 acertou o desenho, e o que sobra é acabamento com consequência.
**Terceira revisão obrigatória, e ela é curta**: o escopo já está fechado por esta lista, e a
única coisa que eu não conseguiria concluir sozinho é o teste de `has_function_privilege` da
SEC-014.

---

## HANDOFF — vetria-seguranca — T-001 (2ª revisão) — 26/08/2026

**Fiz:** releitura integral, do zero, das 738 linhas de `supabase/migrations/0002_nucleo.sql` no
commit `6c34fd9`, sem reaproveitar conclusões da v1. Verifiquei uma a uma as 12 correções
alegadas e respondi às 7 perguntas da coordenação. 9 achados novos: 4 🟠, 4 🟡, 1 bloco ⚪.
Seção "SEGUNDA REVISÃO" acrescentada ao fim de `docs/relatorios/SEC-2026-08-26.md`, com a
primeira revisão preservada inteira.

**Não fiz:** não toquei em `.sql`, `.ts` nem `.tsx`. Não apliquei nada. Não editei
`06-PERMISSOES.md` (SEC-013 continua pendente de decisão registrada). Não consegui executar SQL
contra o banco — os três itens que dependem disso estão listados em "Não consegui verificar", e
o primeiro deles muda a gravidade da SEC-014.

**Estado agora:** **reprovada de novo, mas por outro motivo e em outra categoria.** Os dois 🔴
da v1 estão mortos: SEC-001 fechada nas duas metades, SEC-002 resolvida por separação de tabela
(escolha melhor que a que eu tinha recomendado), e `perfil_privado` resistiu aos quatro vetores
que testei, embed do PostgREST incluído. Os quatro 🟠 da v1 estão fechados. O que impede o `sim`
são quatro 🟠 novos: a busca pública desligada para o anônimo (`:651`), travessia de caminho no
CHECK do documento (`:230`), CRMV editável depois de aprovado (`:337-339`), e o bloco de
reversão que não roda (`:707-708`).

**Descobri:** (a) **três dos quatro 🟠 novos nasceram das correções** — o padrão é o mesmo em
todos: a correção mirou o sintoma exato que eu apontei e passou por cima do contexto em volta;
(b) **revogar `EXECUTE` de função usada em policy `to anon` desliga a policy**, porque a
expressão de policy é avaliada como o usuário da consulta e `SECURITY DEFINER` troca o usuário
só depois da checagem de privilégio — é uma armadilha que vale virar regra permanente do
checklist; (c) **CHECK de prefixo não é CHECK de formato**: `like uuid || '/%'` aceita `..` e a
normalização de URL faz o resto; (d) **um ganho não planejado**: com a SEC-001 fechada,
rebaixar um `vet` para `tutor` agora tira o perfil da busca sozinho, porque `perfil_esta_ativo`
exige o role — antes exigiria limpeza manual; (e) a v2 acertou `is not distinct from` para
`status_motivo` e `=` para `status`, coluna por coluna, o que indica que o padrão foi entendido
e não copiado.

**Bloqueios:** T-001 não aplica até os quatro bloqueantes entrarem. O teste de
`has_function_privilege` da SEC-014 precisa rodar antes, porque define se a SEC-014 quebra só o
visitante anônimo ou todo mundo. T-002 (bucket) depende da SEC-015: escrever policy de Storage
em cima de um `documento_path` que aceita `..` é construir sobre o buraco. F4 inteira depende da
SEC-014. F3/S4 depende da SEC-022 (1), senão o reprovado não alcança a tela onde o motivo aparece.

**Próximo passo óbvio:** `vetria-backend` aplica os quatro bloqueantes mais os cinco de uma linha
no próprio `0002_nucleo.sql` — continua sendo a origem, porque ela segue não aplicada. Antes
disso, rodar o teste de privilégio da SEC-014 num ambiente de teste. Depois, terceira revisão,
curta, com escopo fechado por esta lista.

**Docs que atualizei:** `docs/relatorios/SEC-2026-08-26.md` (seção "SEGUNDA REVISÃO"
acrescentada; primeira revisão intacta). Nada mais.

**Pendente para o `vetria-escriba`:** SEC-014 a SEC-017 viram T-011 a T-014 em `03-TAREFAS.md`;
SEC-018, SEC-019, SEC-021 e SEC-022 (1) entram como subitens do card da T-001; SEC-020 e
SEC-022 (2) viram decisão em `05-DECISOES.md`; SEC-013 segue pendente desde a primeira revisão;
o card da T-001 ganha "2ª revisão de segurança em 26/08/2026 — **reprovada**, 4 🟠, ver
SEC-2026-08-26 §SEGUNDA REVISÃO".

**Commits:** nenhum. Auditor não commita código.
